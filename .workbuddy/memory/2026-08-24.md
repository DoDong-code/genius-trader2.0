# 2026-08-24 — 全链路 NAV/估值 数据架构修复（只读审计 + 前端三态统一）

## 目标
按用户紧急指示：停止打补丁，做一次全链路数据架构修复。Web + mp1 盘中稳定显示三态：
- CONFIRMED_NAV（nav.confirmed===true && date && value>0 → 蓝 MMDD）
- TODAY_ESTIMATE（estimate 有效 → 灰「估值」）
- NO_DATA（灰「暂无数据」）

## 真实验证（Render API，14 只基金 ≥10 要求）
- 后端「今天」= 2026-08-25（周二）。绝大多数基金 latest_nav=2026-08-21（12/14），019633=2026-08-24，012920=2026-08-20。
- 列表接口 `/api/fund/CODE`（无 refresh=1）estimate 一律 null；estimate 仅 `?refresh=1` 返回。
- **结论：用户观察到的「0824 缺失」是真实后端数据导入缺口（多数基金停在 0821，仅 019633 到 0824），非 UI/Badge bug。**
- 焦点基金 007339(1.8623)/380006(1.2238) latest_nav 均为 2026-08-21 → 盘中正确显示蓝「0821」，不会假蓝「0825」。

## 代码改动（3 文件，未 commit/push/deploy）
1. `persistence.js` HYDRATE Block1+Block2：删除硬编码 `isToday`/`isKnownPolluted('2026-08-24')` 降级，改为仅拒绝「未来日期 / confirmed 但无有效正值」。保留最近已确认净值（如 0821），避免每次刷新清空造成 NO_DATA 闪退。
2. `live-estimates.js` getNavDisplayState：增加 `value>0` 守卫，封堵 res.date 存在但 nav=0/null 误判为蓝（旧 0825 假蓝根因）。
3. `mp1/pages/portfolio/portfolio.js` navConfirmed：后端 latest_nav 无 confirmed 字段，改为由 `ln.date 存在 && ln.nav>0` 推导已确认事实，与 Web 对齐，消除 Web/mp1 不一致。

## 验证
- node --check 5 文件全 PASS。
- mp1/test-regression.mjs：39/39 PASS。
- 服务器测试：calibration/estimateFallback/external 等 8 项失败，均为 server 端（与本次前端改动无关，未修改测试）。
- P3.3-H（navCacheService.js）未改动（FROZEN）。无 merge conflict。

## 死代码清理结论
前端已无 markProviderTodayBadge/markProviderUpdated/providerDisplayName/latestAvailableDate/computeDataBadge/estimate→confirmed 等引用。仅剩 mp1/utils/tradingDay.js 的 `inferDataStatusFromEstimate`（portfolio.js:545 作后端 dataStatus 缺失时的安全 fallback，只返回 PROVIDER_TODAY/STALE/NO_DATA，永不产生 CONFIRMED_NAV，不影响蓝逻辑）→ 保留。

## 待办（用户侧，非本次范围）
- 后端 import/ensureTodayNav 需把 0824/0825 NAV 全面补齐（属后端数据同步缺口）。
- 用户用 GitHub Desktop 自推送；agent 不 push。
