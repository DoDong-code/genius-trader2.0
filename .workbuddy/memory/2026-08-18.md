# 2026-08-18 工作日志

## 首页行情中心折叠态轮播
- `pages/index/index.wxml`：行情模块折叠时从「固定前 2 项」改为循环轮播窗口（用 `marketCollapsedItems` 渲染 2 个卡片），展开时仍遍历全部 `marketIndices`。
- `pages/index/index.js`（纯 UI，未碰接口/数据/计算）：
  - 新增 `marketCarouselIdx` / `marketCollapsedItems` 状态。
  - 双击折叠/展开时启动/停止轮播；`startMarketCarousel` 用 `setInterval(…,1000)` 每 1s 推进起点，`recomputeCollapsedItems` 取连续 2 项（末尾循环）。
  - `fetchMarketIndices` 的 setData 回调在折叠态下重算窗口；`onShow` 恢复轮播、`onHide`/`onUnload` 与切到非「全部」Tab 时暂停。
  - 指数 ≤2 个时不轮播，直接全展示。
- 校验：`node --check` 通过。需在微信开发者工具编译确认折叠态每 1s 轮换两个卡片、展开恢复全部。

## 4 项 UI 微调（账户分段间距/右上角账号显示/登录返回键/设置页点击）
- ① 账户分段 `.account-segmented` 黑灰间距 `gap` 与 `padding` 由 10rpx → 8rpx（`pages/index/index.wxss`、`pages/portfolio/portfolio.wxss`，黑色块大小不变、灰色块同步缩小）。
- ② 删除首页/持仓页右上角账号显示（`auth-entry`：邮箱 /「登录 / 注册」），仅保留左上角 `brand`。`pages/index/index.wxml`、`pages/portfolio/portfolio.wxml`。
- ③ 登录页增加左上角返回按钮 `‹`，`bindtap="goBack"`（login.js 已有 goBack：navigateBack，失败回 index）。新增 `.back-btn` 绝对定位在状态栏下方（`pages/login/login.wxml`、`pages/login/login.wxss`）。
- ④ 设置页「菜单 + 登录按钮无法点击」修复：设置页顶部占位 `.topbar` 为纯装饰 `position: fixed` 白条，加 `pointer-events: none`，确保不拦截下方内容点击。`pages/setting/setting.wxss`。
  - 排查：设置页 wxml/wxss 无全屏遮罩；`request.js` 的 `wx.showLoading({mask:true})` 均有 success/fail 收尾（30s 超时），不会永久卡住。若改后下方分组仍点不动，则为设备/运行时级问题（如前页残留 loading），需截图或 console 日志进一步定位。
- 校验：WXML/WXSS 改动，需在微信开发者工具编译验收。

---
## Phase 3 净值显示一致性最小修复（前端 only，已完成未部署）
- 目标：小程序列表/详情抽屉的「净值日期、今日收益、今日收益率、详情当日净值」与网页端体验一致。仅改小程序前端，未动后端/API/DB/算法/Phase1-2 PG 迁移。
- 改动 4 文件：
  - `mp1/pages/portfolio/portfolio.js`：`filterAndSortFunds` 增加 officialUpdated 判定（cached.navDate===expectedNavDate 且 officialChange finite，QDII 取前一交易日）；命中时今日收益率=todayProfit=amt×officialChange，与蓝色「已更新」徽章同源；否则保持原 estimate/f.today。排序/过滤未改。
  - `mp1/components/fundDetail/fundDetail.js`：新增 `_resolveCurrentNav(history)`，利用已有 `serverHistory` 推导成对字段 `currentNav`/`currentNavDate`（末条即最新 NAV，自动覆盖周末/未公布场景），并据最新两条 NAV 重算今日收益/收益率，覆盖本地可能过期的 `fund.today`。
  - `fundDetail.wxml`：header 新增「当日净值 / 净值日期」行（`wx:if currentNavDate`）。
  - `fundDetail.wxss`：新增对应样式（复用 CSS 变量）。
- 验收：node --check 两 JS 全过；grep 确认未误改 server/ 后端；无 mp1 单测（项目无小程序测试）；静态场景脚本 16 断言全 PASS（场景 A/B/C/D + QDII）。
- 残留风险：列表非 officialUpdated 分支（NAV 未公布/周末/跨午夜未刷新）的「今日收益」仍按指令保留 estimate/f.today 旧逻辑；因该分支徽章为灰「估值」（无显式日期），不会出现「日期A+NAV B+收益C」错位，estimate 刷新后自愈。详情抽屉因改用 history 推导，始终自洽。
- 未部署、未 push（等用户验收后决定）。

## 开发者工具「卡死/点不动」根因与修复（dev-tools only，真机正常）
- 现象：真机测试正常，仅微信开发者工具模拟器卡住、界面点不动。已排除功能 bug 与卡住的 `wx.showLoading({mask:true})`（后者真机同样会卡）。
- 根因：**开发者工具 Electron 单线程渲染瓶颈**被两件事放大——
  ① `utils/request.js` 给**每个请求**都 `console.log('[request] wx.request ->', fullUrl)`（日志里 fetch 类请求上百条），每条 log 在 devtools 都要做 `[sm]` sourcemap 解析 + 同步写入 Console 面板，累积成本极高；
  ② 首页/持仓页 `onShow` 每次重发一批请求（首页 `/api/market/indices`；持仓页 `refreshData`→`_refreshNavDatesIfNeeded` 每只基金 `/api/fund/:code?fast=1`，一次 15 并发）。tab 页 switchTab 不销毁，`getCurrentPages()` 中多页共存，`app.notifyAccountsChanged()` 会同时触发多页 `refreshData`。
- 修复：`request.js` 的每请求 log 改为**默认关闭、按需开启**——`if (wx.getStorageSync('debug_requests')) console.log(...)`。需要看网络时在 Console 执行 `wx.setStorageSync('debug_requests', true)` 即可恢复。纯日志开关，无功能/数据/接口变化。
- 仍残留的临时调试日志（已标注「诊断后删除」）：`app.js` 的 `[Auth]/[Sync]/[Verify]/[Login-Debug]`、`index.js`/`login.js` 的 `[Login-Debug]`。单次、低频，非性能杀手，后续清理即可。
- 校验：`node --check utils/request.js` 通过。

## 切 tab「3↔4 数量变化」修复（长按设置页切换隐藏「分析」tab 后，切 tab 数量跳变）
- 现象：长按设置 tab 10s 切换 experimentalMode 后，切其他 tab 时底部栏可见 tab 数会在「3 / 4」之间变（关闭时跳 3↔4，打开时跳 4↔3）。初版用 `wx:if` 过滤分析项 → 仍闪/仍变。
- 真正根因：旧实现把「分析」tab 的显隐做成**重建 list 数组**（开=4 项 / 关=过滤掉分析=3 项）。只要模式变化或 `applyExperimentalMode` 被触发，列表长度就变、flex 重新分列 → 数量跳变。
- **最终、最稳修法（v3，已取代内联表达式方案）**：`list` 永远是完整 4 项、永不重建；「分析」tab 显隐由每个 item 的 `hidden` 字段（**在 JS 里预计算**）控制 + CSS `.tab-bar-item.is-hidden{display:none}`。
  - `custom-tab-bar/index.js`：新增 `buildList(enabled)` 用 `FULL_LIST.map(i => ({...i, hidden: i.pagePath==='/pages/analysis/analysis' && !enabled}))`（JS 预计算 hidden，避免 WXML 内联复杂 `&&`/`?:` 表达式的求值不确定性）。`data.list = buildList(false)`（恒 4 项）；`data.experimentalOn`；`attached` `setData({experimentalOn:on, list:buildList(on)})` 读 storage 初始化；`applyExperimentalMode(enabled)` 带相等守卫仅切 `experimentalOn`+`list`（**只在启动+长按切换时调用，切 tab 不触发**）；`highlight` 只改 `selected`；`switchTab` 新增双保险——`path==='/pages/analysis/analysis' && !experimentalOn` 时直接 `return`（隐藏的分析 tab 不可点，防止误触弹回总览）。
  - `index.wxml`：`wx:for="{{list}}"` 恒 4 项；分析项 `class` 改为 `{{item.hidden ? 'is-hidden' : ''}}`（简单布尔绑定）。
  - `index.wxss`：`.tab-bar-item.is-hidden{display:none}`（提权盖过 `.tab-bar-item{display:flex}`）。
  - **切 tab 永远只改 `selected`、永不碰 list 长度 / 显隐** → 可见数量只在「长按切换那一下」变化。4 个 tab 页 `onShow` 仍只调 `highlight`。`analysis.js` 关闭时停留此页跳回总览重定向保留。
- ⚠️ **关键环境坑（排查时务必优先排除）**：微信开发者工具对 `custom-tab-bar` 自定义组件**经常热重载不生效**（改了 js/wxml/wxss 但跑的是旧包）。若改完仍「关闭还是 4 个」，先做：**开发者工具 → 清缓存 → 全部清除 → 重新编译**（或重启 devtools），再验证。真机无此问题。
- 校验：`node --check custom-tab-bar/index.js` OK；grep 确认 WXML 无内联 `pagePath===...&&!experimentalOn` 表达式、JS 无 `fullList`/`list.filter`/`pageLifetimes`/多余 `applyExperimentalMode`。

---
## Phase 3.1 估值引擎异常专项 · 只读审计结论（未改代码）
- 症状①网页「本地引擎估值」几乎全 0；症状②微信切第三方后「无数据」。
- 根因定位（两条独立链路）：
  1. 症状②= 微信端短名 bug：`mp1/pages/portfolio/portfolio.js:434` 发 `source=xbyj/yjb`，但 `server/services/providerEstimate.js` 用 `PROVIDER_ORDER=['xiaobeiyangji','yangjibao']` 直接 `===` 过滤 → 永远空 → `fetchProviderEstimate` resolve(null) → 服务器回退 `calculateFundEstimate`(本地引擎) → 本地又 null → `estimate_change` 非有限 → `updated=0` → 弹「无新数据」。网页端 `preferredEstimateSource()` 发内部名故网页 provider 正常。
  2. 症状①= 本地引擎 `calculateFundEstimate` 返回 `estimate_change:null`（pricedHoldings 空→holdingsChange null + sectorChange null[getFund Promise bug] + public estimate 也失败）→ 网页 `live-estimates.js:362` `Number(null)=0` 且 0 有限 → 穿透 `!isFinite` 校验 → 显示「0%」而非「无数据」。
- 关键判断：沙箱 curl eastmoney push2 正常返回（茅台 f170=-34）→ 非全局故障；生产 Render 出口 IP 疑似被 eastmoney 封锁/限流 → 本地引擎实时行情全失败 → null→0。需在 Render 侧查 `[marketService]`/`[stock-history]` 日志或临时从 Render 探活确认。
- Phase 1/2 是否引入：否。仅 (a) Phase2 回归 `estimateEngine.js:269/275` `getFund` 变 async 但未 await（fund 成 Promise，导致 name/sector/QDII targetDate 错误、sectorChange 恒 null，非 0 根因但应修）；(b) stock_price 仍走 SQLite(getDatabase) 而 PG 有数据=真实读写分离，但本地引擎只用 stock_price 作当日行情缓存、未命中即实时抓，故非 0 根因（benign）。fund_nav/fund_holdings 均已走 PG ✅。
- 最小修复（待确认）：providerEstimate.js 加 xbyj→xiaobeiyangji/yjb→yangjibao 别名；live-estimates.js:362 把 null/非有限当 unavailable；estimateEngine.js:269/275 改 `await getFund`。本地引擎 Render 不可用属环境/依赖问题，根治需换非 eastmoney 行情源或默认改用 provider，超出最小范围，需决策。
- 红线遵守：全为只读审计，未改代码/库/API、未 sync/calibrate/部署/导入。

---
## Phase 3.1 估值引擎异常专项 · 修复阶段（3 项最小修复，已完成未部署）
- 基于只读审计，仅实施 3 项最小修复，严守红线（不动 DB/结构/数据、不 sync/calibrate、不导入、不改估值算法/行情源/默认源/API 返回结构、不部署、不扩范围）。
- 修复 1 · `server/services/providerEstimate.js`（微信第三方源别名映射）：
  - 新增 `const SOURCE_ALIASES = { xbyj: 'xiaobeiyangji', yjb: 'yangjibao' }`。
  - `fetchProviderEstimate` 内 `options.source` 先经 `SOURCE_ALIASES[String(options.source)] || options.source` 归一化后再 `PROVIDER_ORDER.filter(name => name === source)`。内部名原样透传（网页端行为不变），provider 顺序/返回结构/第三方接口/凭证逻辑均不动。
- 修复 2 · `live-estimates.js:362`（网页 null 估值误显 0）：
  - 原 `var change = Number(estimate && estimate.estimate_change)` —— `estimate_change===null` 时 `Number(null)=0` 且有限 → 穿透 `!isFinite` → 误显 0%。
  - 改为先取 `rawChange`，仅当 `rawChange !== null && !== undefined` 时才 `Number()`，否则置 `NaN`；`NaN/Infinity/-Infinity` 均 `!isFinite` → 走 `showEstimateUnavailable`（显示「— / 待估值」）。真实 0% 仍正常显示。
  - 未改有效数字显示/排序/估值计算/API；第三方有效估值不受影响。
  - 记录未改：同文件 `live-estimates.js:505` 的 `var pChange = Number(pv && pv.estimate_change)`（延迟第三方纠偏路径）含同类 null→0 隐患，按「额外同类只记录不扩改」规则保留。
- 修复 3 · `server/services/estimateEngine.js`（Phase2 async 回归）：
  - `calculateFundEstimate` 内两处 `getFund(fundCode)`（269、275 行）改为 `await getFund(fundCode)`；该函数本就 `async`，仅补 await，未改 sectorForFund/QDII 判断/估值算法/返回结构。
- 验收：
  - `node --check` providerEstimate.js / estimateEngine.js / live-estimates.js 均 OK。
  - grep 确认 SOURCE_ALIASES 映射、两处 `await getFund` 均已生效；旧 `Number(estimate && estimate.estimate_change)` 已移除。
  - 全仓 `getFund(` 调用核查：estimateEngine 2 处已 await；portfolioAnalysisService.js:214、fundService.js:451 本就 await；fundService.js:460 为定义。无额外同类型未 await 漏洞（estimateEngine 内 getRealtimeFundEstimate/calibrateFund/importFund/fetchStockQuote/latestHoldings 均已 await）。
  - 运行 `node --test server/tests/*.test.js`：**4 个测试文件全 FAIL**，根因为环境缺依赖 `pg`(^8.23.0)、`qrcode`(^1.5.4)（package.json 已声明但 node_modules 未装），且本沙箱无 PG 连接/凭据 → 非本次修改导致，属既有环境问题。本次 diff 仅 +14/-4 行逻辑，无任何新增 `require`。
- 结论：本地引擎 Render 上 `estimate_change:null`（Root Cause ① eastmoney 行情在 Render 出口被封）**本阶段按指令未修**；修复 2 仅保证 null 在网页显示「无数据」而非「0%」。3 项最小修复完成、安全（未触 DB/API/算法/部署），可进入下一步生产验证（用户在 GitHub Desktop 提交→push→Render 部署后，复测微信第三方估值 + 网页 null 显示）。
- 红线遵守：未部署、未 sync/calibrate/导入、未动库/API/算法/行情源。

---
## Phase 3.1.1 生产数据层只读审计（2026-08-18，纯只读，未改代码/库/部署）
- 生产新发两类错误：① `GET /api/fund/:code/estimate` → `Error: FOREIGN KEY constraint failed at calculateFundEstimate (estimateEngine.js:460)`（022184/018147/014002/016665 同错）；② `Error: timeout exceeded when trying to connect at pg-pool → dbAsync.js:41/48 → getFund → getRealtimeFundEstimate → calculateFundEstimate`，随后 `Node.js process exited` / Render 重启。
- **P0-A · SQLite fund_estimate 外键断裂（Phase2 架构断裂，确定性 API 500）**：
  - `db.js:14` `PRAGMA foreign_keys=ON`；`db.js:65-80` `fund_estimate` 有 `FOREIGN KEY(fund_code) REFERENCES fund(fund_code)`（指向**同库 SQLite fund**）。
  - 读写归属：`fund`=`getFund`(fundService.js:460→dbAsync=**PG**)；`fund_nav`=getFund.latest_nav/fund API(**PG**)；`fund_holdings`=`latestHoldings`(estimateEngine.js:35→dbAsync=**PG**)；`fund_estimate`=estimateEngine.js:242 读 / :445 写(`getDatabase()`=**SQLite**)。
  - 写 SQLite `fund` 的代码核查：importFund(fundService.js:353，在 dbAsync 事务内=PG)、portfolioService.js:9-20(dbAsync=PG)；grep 全仓无 `getDatabase()` 写 fund 表 → 云模式 SQLite `fund` **存在但永不被写→空**。
  - 故 estimateEngine.js:445 向 SQLite `fund_estimate` INSERT 真实 fund_code 时 FK 找不到父行 → 失败→抛错→index.js:183 catch→500。缓存永不写入→每只基金必现。
  - 关键佐证：`ensureCloudSchema`(dbAsync.js:131-265) 建 PG 表清单含 fund/fund_nav/fund_holdings/portfolio/stock_price/fund_calibration，**不含 fund_estimate** → fund_estimate 是遗留 SQLite 孤儿表。
- **P0-B · PG 连接超时→进程退出（Phase2 放大，健壮性问题）**：
  - `dbAsync.js:20-30` 单例 Pool：`max:5, connectionTimeoutMillis:10000, ssl.rejectUnauthorized=false`，无 `idleTimeoutMillis/min`；**无 `pool.on('error')`**。index.js 仅 SIGINT/SIGTERM，**无 `process.on('uncaughtException'/'unhandledRejection')`**。
  - 单例→非每请求建池；简单查询/事务均释放连接→无代码级泄漏；ensureCloudSchema 仅启动 await 一次→不长期占连接。
  - 错误文案 `timeout exceeded when trying to connect` 对应**建连(TCP/TLS)超时 10s**，非 acquire 等待超时。类型=A(PG 网络建连失败/慢)；触发/放大=Phase2 把 fund/nav/holdings 迁 PG 后单请求 PG 往返由 0 增至多次，max:5 小池+空闲回收在并发 estimate 突发(Render 冷启/闲置唤醒)时被迫频繁新建连接→建连超时。非纯净池耗尽(acquire)、非 PG 连接数上限(too many connections)、非泄漏。
  - 进程退出机制：PG 连接错误若以空闲/建连事件抛出，因缺 `pool.on('error')` 与 process 兜底→unhandled 'error'→Node 终止→Render 重启（请求内 await 错误已被 index.js try/catch 兜成 500，不会直接崩进程）。
- **P2 · SOURCE_SECRET_KEY**：`crypto.js:14-16` 用于 AES-256-GCM 加密第三方凭证；生产未设则 warn 用开发默认密钥（安全隐患）。**与 PG timeout、FK 均无直接关系**。
- **结论分级**：P0=① FK 断裂(确定性 500，Phase2 断裂) ② PG 超时+缺 handler→进程退出；P1=进程退出根因(PG 错误无处理器+Phase2 PG 重负载)；P2=SOURCE_SECRET_KEY；暂缓=Eastmoney 行情/本地引擎策略/Phase3.1 source alias 验收（alias 修好但当前被 FK 500 掩盖：provider 命中不走本地写故可用，本地回退仍撞 FK）。
- 红线：纯只读审计，未改代码/库/表/缓存/部署。待用户确认修复方案（fund_estimate 迁 PG 或禁用该 FK；dbAsync 加 pool.on('error')+process 兜底；配 SOURCE_SECRET_KEY）。

---
## Phase 3.1.2 生产估值链路 P0 修复（基于 3.1.1 审计，已完成代码改动，待部署）
- 严格 scope：只修审计定位的 P0（fund_estimate SQLite/PG 混用 + PG pool 无 error handler）；不动估值/持仓/行业/校准算法、Eastmoney 行情、provider 顺序、API 返回结构、净值算法；不 sync/calibrate/import；不删 SQLite 表；不 DROP PG 数据。
- 修复 A · `server/database/dbAsync.js` `ensureCloudSchema` 新增 **PG `fund_estimate` 表**（按 SQLite schema 原样迁移字段/类型/UNIQUE(fund_code,trade_date)/索引；外键改指向 **PG `fund(fund_code)` ON DELETE CASCADE**，与 fund_nav/fund_holdings 一致；云模式不再依赖空的 SQLite fund）。
- 修复 B · `server/database/dbAsync.js` `getPool()` 单例 Pool 增加 `pool.on('error', err=>console.error(...))`——PG idle/connection 错误不再冒泡为 unhandled 'error' 致 Node 退出（Render 重启）。保留单例架构、`connectionTimeoutMillis:10000`（有限，未无限等待）；不吞查询异常（await 失败仍按原路径抛→API 500）。
- 修复 C · `server/services/estimateEngine.js`：`cachedEstimate`（读）由 `getDatabase().prepare().get()` 改 `await dbAsync.get()` 并变 async，调用点 :296 加 `await`；写入 :445 由 `getDatabase().prepare().run()` 改 `await dbAsync.run()`（INSERT...ON CONFLICT(fund_code,trade_date) DO UPDATE，PG 兼容）。`dbAsync.get/run` 内部按 `isCloud()` 分支→云走 PG、本地回退 SQLite，本地行为不变。
- **未动**（明确 out-of-scope，按"禁止扩范围"保留）：estimateEngine.js:48/66 的 `stock_price` SQLite 缓存/写入（benign、无 FK）；:475 `calculateAccountEstimate` 的 `portfolio JOIN fund` SQLite 读（账户估值端点，不在 4 只基金 /estimate 验证路径）。
- 验收（本地）：
  - `node --check` dbAsync.js / estimateEngine.js 均 OK。
  - grep 确认 estimateEngine 仅 :242/:445 的 fund_estimate 读写改走 dbAsync；pool.on('error') 已在 :31；PG fund_estimate DDL 已在 :269-284（FK:271 指向 PG fund）。
  - 运行 `node --test`：**4 测试文件仍全 FAIL**，原因同 3.1——`node_modules` 缺 `pg`/`qrcode`（dbAsync 顶层 require('pg')），沙箱无 PG 连接/凭据；本次 diff 无新增 require，非本次引入。
  - SOURCE_SECRET_KEY：代码 `crypto.js:14-16` 保留缺失 warning（未改）；生产是否配置需用户在 **Render 控制台**确认（agent 无法读取 Render 环境变量），且必须用迁移前原值、禁止重新生成/改历史凭证。
- **待部署后（用户在 GitHub Desktop 提交→push→Render 自动部署）才可执行**：① 确认 PG 已建 fund_estimate（启动日志 `[dbAsync] PostgreSQL schema ready`）；② 只读验证 4 只基金 `GET /api/fund/{022184,018147,014002,016665}/estimate` 不再 FOREIGN KEY constraint failed、进程不再因 PG error exit 1；API 返回有效 estimate 或明确 unavailable（不把异常转 0%）。
- 残留已知（非本次、暂缓）：Eastmoney 行情在 Render 出口疑似被封→本地引擎 `estimate_change:null` 仍可能；届时再单独验证 PG/fund/holdings/estimate/provider/本地 engine 链路。
- 红线遵守：未部署、未 sync/calibrate/导入、未动库结构/数据/算法/行情源。

---

## 切 tab「3↔4 数量变化」再次修复（v6：list 去除 hidden 快照，显隐改 WXML 实时算）
- 背景：v5 把分析 tab 显隐做成「list 里每个 item 的 `hidden` 快照字段 + CSS is-hidden」。用户实测（devtools + 真机均复现，已排除缓存）：`experimentalMode=false` 初始 3 个正常，但**点击总览/持仓后变成 4 个**；`experimentalMode=true` 始终稳定 4 个。
- 全项目搜索结论：`getTabBar()` 仅 4 个 tab 页 `onShow` 调 `highlight()`，无任何页面调 `applyExperimentalMode` 或重建 list；`FULL_LIST/buildList/experimentalMode` 仅出现在 `custom-tab-bar/index.js`、`analysis.js`、`setting.js`；**无 `FULL_LIST.filter(...)`、无第二处 `.setData({list`**。即静态代码没有二次重设 list。
- **真正根因（v6 判定）**：把 `hidden` 作为**快照写进 list 项**，WXML 用 `item.hidden` 决定是否加 `is-hidden`。微信自定义 tabBar 组件在切 tab / `getTabBar()` 拿到尚未 `attached` 的实例等运行时，组件 `data` 可能被重置/重新初始化，该**快照字段丢失或变 `undefined`** → `item.hidden ? 'is-hidden'` 为假 → 分析 tab 失去 `display:none` → 显示成 4 个。切 tab 只改 `selected` 不会主动清 list，但任何组件重建都会让「快照」失效。
- **最终修法（v6，守住 v5 全部约束）**：`list` 永远只存静态 `FULL_LIST`（4 项，**不再有 hidden 字段、不再用 buildList**）；显隐的唯一真源是 `experimentalOn`；**WXML 直接实时计算** `item.pagePath === '/pages/analysis/analysis' && !experimentalOn ? 'is-hidden' : ''`。任何渲染都重新派生，永不脱钩。
  - `custom-tab-bar/index.js`：`data.list = FULL_LIST`；删除 `buildList` 函数；`attached` 只 `setData({experimentalOn:on})`；`applyExperimentalMode` 只改 `experimentalOn`；`highlight` 只改 `selected`；`switchTab` 双保险拦截隐藏分析保持不变。保留 4 处 `[TAB ...]` 诊断日志（attached/applyExperimentalMode/highlight/switchTab），日志里 hidden 改为按 experimentalOn 实时派生以便真机核对。
  - `index.wxml`：class 改为实时表达式（去掉 `item.hidden`）。
  - `index.wxss`：`.tab-bar-item.is-hidden{display:none}` 不变。
- 验收：`node --check custom-tab-bar/index.js` OK；grep 确认 `custom-tab-bar` 目录内无 `item.hidden` / `buildList` 残留。真机/模拟器 3↔4 验证需用户编译后按「关闭态连续 总览→持仓→设置→… 恒 3；开启态连续 … 恒 4」核对；诊断日志确认 `experimentalOn:false` 且 `analysis.hidden:true` 始终不变即可。
- 注：诊断日志为临时，验证通过后建议删除 4 处 `[TAB ...]` console.log。

## 切 tab「3↔4 数量变化」最终修复（v7：多实例 experimentalOn 漂移，统一回 storage 真源）
- **最新实测（devtools + 真机均复现，已排除缓存与 v6「hidden 快照丢失」假设）**：`experimentalMode=true` 始终稳定 4 个；`experimentalMode=false` 初始 3 个正常，但点击总览/持仓后变 4 个。日志显示 `[TAB switchTab] experimentalOn:true` 紧接 `[TAB highlight] experimentalOn:false`，且多次出现 `[TAB attached]`。
- **真正根因（v7 判定，与 v6 不同）**：微信自定义 tabBar 在不同 tab 页面切换过程中会**多次 `attached`（多实例）**。`this.data.experimentalOn` 只是「某个 tabBar 实例自己的缓存状态」，不是全局真值。长按设置 tab 翻转 storage 时（在实例 A 上 `applyExperimentalMode` 刷新），**实例 B 的 `experimentalOn` 仍停留在旧值**；下次切到 B 时 v6 的 `highlight` 只改 `selected`、`switchTab` 只用 `this.data.experimentalOn` 作真值 → B 用旧缓存判断 → 分析 tab 显隐漂移 → 3↔4 跳变。本质是「实例间 experimentalOn 不一致」。
- **最终修法（v7，守住全部 8 条 v5 约束 + 产品语义）**：
  - 新增 `getExperimentalMode()` = `Boolean(wx.getStorageSync('experimentalMode'))`，作为**唯一真实状态源**；`experimentalOn` 降级为「本实例 UI 缓存」。
  - 恢复 `buildList(enabled)`（`FULL_LIST.map` 给分析项打 `hidden`，**恒 4 项、绝不 filter**）；`data.list = buildList(false)`。
  - `attached` / `highlight` / `switchTab` / `applyExperimentalMode` **每个方法在用到显示状态时都先 `getExperimentalMode()` 从 storage 重同步**再 `setData({experimentalOn, list:buildList(on)})`（highlight 顺带改 selected）。这样无论哪个实例被激活，落地的 `experimentalOn/list` 都来自 storage 真值，实例间不再漂移。
  - `applyExperimentalMode` 成为**写 storage 的唯一入口**（onTabTouchStart 不再直写，改为取反 `getExperimentalMode()` 后调 applyExperimentalMode）；其他实例在下次 highlight/switchTab 自动从 storage 同步。
  - WXML **回退**到 `{{item.hidden ? 'is-hidden' : ''}}`（保持 `buildList` 生成的 hidden 字段驱动，不引入新渲染机制）；CSS `.tab-bar-item.is-hidden{display:none}` 不变。
  - 守护 4 处 `[TAB ...]` 诊断日志（attached/highlight/switchTab/applyExperimentalMode），日志字段同步改 `storageExperimentalMode` 以核对漂移是否消失。
- 验收：`node --check custom-tab-bar/index.js` OK；grep 确认 `custom-tab-bar` 目录 `item.hidden` 已恢复 WXML、`this.data.experimentalOn` 仅剩 switchTab 内「是否需重同步」比较（非真值来源），无 `FULL_LIST.filter`。真机/模拟器按「关闭态连续切 tab 恒 3、开启态恒 4、长按动态切换后继续切 tab 保持」验收；诊断日志确认 `storageExperimentalMode` 与 `experimentalOn` 始终相等且全程不变即可。验证通过后建议删除 4 处 `[TAB ...]` console.log。

## 切 tab「闪两次后恢复」修复（v8：切 tab 只改 selected，不再重同步显隐）
- **现象**：v7 修好「关闭态切 tab 永久变 4 个」后，出现新问题——切任意其他 tab 时 tabBar 会**闪两次再恢复正确状态**（devtools + 真机均复现）。日志已排除缓存、排除 experimentalMode 丢失。
- **真正根因**：v7 在 `highlight()` 和 `switchTab()` 里每次切 tab 都 `getExperimentalMode()` → `setData({experimentalOn, list:buildList(on)})`，导致**切 tab 反复重算 list + 重渲染隐藏状态**（多实例下尤其明显），表现为闪两次。
- **最终修法（v8，守住全部 8 条 v5 约束 + 产品语义）**：
  - 状态分层：storage = 唯一真值；experimentalOn/list.hidden = 实例 UI 缓存。
  - **切 tab 只改 selected**：`highlight(pagePath)` 仅 `findIndex` → `if(idx<0||idx===selected)return` → `setData({selected})`，**禁止**读 storage / `buildList` / 改 `experimentalOn` / 改 `list`（已 grep 确认无残留）。
  - `switchTab(e)` 只在「点击分析」时读一次 storage 做权限拦截（`!on` 则 return），**无任何 setData / buildList / 改 experimentalOn / 改 list**。
  - 只有**两处**允许动 experimentalOn/list：`attached`（初始化）+ `applyExperimentalMode`（长按真正切换，且成为**唯一写 storage 入口**——`onTabTouchStart` 改为取反 `getStorageSync` 后只调 `applyExperimentalMode`，不再直写 storage）。
  - 删除 `getExperimentalMode()` 辅助方法（仅初始化/拦截/取反处内联 `Boolean(wx.getStorageSync(...))`）。
  - 日志精简：attached（`experimentalMode/experimentalOn`）、highlight（`pagePath/selected`）、switchTab（`path`）、applyExperimentalMode（`enabled`）。普通切 tab 不再打印 experimentalOn / buildList。
  - WXML 结构不变（`{{item.hidden ? 'is-hidden' : ''}}`），CSS `.tab-bar-item.is-hidden{display:none}` 不变。
- 验收：`node --check custom-tab-bar/index.js` OK；grep 确认 `getStorageSync` 仅出现于 attached(初始化)/switchTab(点分析拦截)/onTabTouchStart(取反)；`buildList` 调用仅 data 初始化/attached/applyExperimentalMode；`setData({experimentalOn,list})` 仅 attached(40) 与 applyExperimentalMode(53)，highlight 的 setData 仅 `{selected}`，switchTab 无任何 setData。真机/模拟器按「关闭态连续切 tab 恒 3 且不闪、开启态恒 4 且不闪、长按动态切换后继续切 tab 保持且不闪」验收。验证通过后建议删除 `[TAB ...]` 诊断日志。

---

## 切 tab 多实例状态漂移最终修复（v9：App 全局唯一状态 + 实例广播）
- **最新真机日志根因**：微信小程序存在**多个 custom-tab-bar 实例**（index/portfolio/setting 各一个，各自 attached 都打印 experimentalMode=false）。v8「attached + applyExperimentalMode 只改本实例」方案下，长按设置只更新了 setting 自己的 tabBar 实例，切回 index/portfolio 时它们仍是旧值 → 开启后切回变 3、关闭后切回变 4。实例间不共享状态。
- **最终修法（v9）**：建立 **App 全局唯一实验模式状态**，所有实例共享。
  - `app.js` `globalData` 新增（不覆盖现有字段）：`experimentalMode: Boolean(wx.getStorageSync('experimentalMode'))`、`tabBarInstances: []`（实例注册表）。
  - `custom-tab-bar/index.js`：
    - `attached`：优先读 `app.globalData.experimentalMode`（布尔则为真值；否则读 storage 并回填 globalData）→ 注册本实例到 `globalData.tabBarInstances`（仅 attached 注册，不在页面 onShow 注册）→ `setData({experimentalOn, list:buildList(on)})` + 日志。
    - 新增 `detached`：从 `tabBarInstances` 过滤注销本实例，防持有已销毁实例。
    - `applyExperimentalMode(enabled)` 成为**唯一模式切换入口**：`wx.setStorageSync` 持久化 → 写 `globalData.experimentalMode` → 遍历 `tabBarInstances.slice()` 对每个实例 `setData({experimentalOn, list:buildList(on)})` 广播 → 日志含 `instanceCount`。这样长按设置后 index/portfolio/setting 全部同步。
    - `highlight` 保持 v8（只 `setData({selected})`，禁 storage/buildList/改 experimentalOn/list）。
    - `switchTab` 保持 v8，但点分析时优先读 `globalData.experimentalMode`（回退 storage）做权限拦截，绝不 setData。
    - `onTabTouchStart` 取反全局状态后只调 `applyExperimentalMode`，不再直写 storage（写 storage 统一归 applyExperimentalMode）。
  - WXML 结构不变（`{{item.hidden ? 'is-hidden' : ''}}`，CSS `.tab-bar-item.is-hidden{display:none}`）。
- 验收：`node --check index.js` + `app.js` 均 OK；grep 确认 `setData({experimentalOn,list})` 仅 attached(52) 与广播(86)，highlight 仅 `{selected}`(99)，switchTab 无 setData，`setStorageSync` 仅 applyExperimentalMode(74)。真机/模拟器按「关闭恒3/开启恒4、切回其它 tab 保持、长按动态切换后所有实例同步且不闪」验收；日志确认 `[TAB applyExperimentalMode] instanceCount≥已创建实例数`。验证通过后建议删除 `[TAB ...]` 诊断日志。

---

## Phase 3.1.2.1 线上 P0 修复回溯审计（只读，未改代码/库/部署）
- 现象：Render 部署后 `/api/fund/:code/estimate` 仍报 `Error: FOREIGN KEY constraint failed code: ERR_SQLITE_ERROR at estimateEngine.js:460:6`；启动日志 `[dbAsync] PostgreSQL schema ready` + `Database: PostgreSQL (cloud)`。
- **关键判定（代码证据，非猜测）**：`code: ERR_SQLITE_ERROR` 是 `node:sqlite` 原生错误码；PostgreSQL(pg) 外键冲突码是 `23503`，二者绝不混淆 → 线上必在执行真正的 SQLite 写入。
- **当前仓库（HEAD=b1f0927 ".2"，工作树干净，修复已提交）**：
  - `estimateEngine.js:460` = `  `, [`（属 `await dbAsync.run(...)` 的 PG 参数数组）；若出错必是 pg `23503`，不可能是 `ERR_SQLITE_ERROR`。
  - `estimateEngine.js` 中 `fund_estimate` 读 :244（dbAsync.get）、写 :445（dbAsync.run）；全仓 `getDatabase().prepare` 位置（dbAsync.js:50/67 本地回退、estimateEngine.js:48/66 stock_price、:475 portfolio、fundService.js:211/221、estimateService.js:79、test:66）**无一涉及 fund_estimate** → 当前仓库云模式无 SQLite fund_estimate 路径。
  - `dbAsync.js:269-284` 已在 ensureCloudSchema 建 PG `fund_estimate`（FK:271 指向 PG `fund(fund_code)`）；`isCloud()=Boolean(DATABASE_URL)`，Render 有 DATABASE_URL → dbAsync.run/get 必走 PG，无 SQLite fallback（无 try/catch 回退）。
- **旧提交 HEAD~1（修复前）实证**：`estimateEngine.js:445` `getDatabase().prepare(`、`:460` `  `).run(`（SQLite 写入）→ 与生产错误 `:460` + `ERR_SQLITE_ERROR` **精确吻合**。
- **最终根因 = A：Render 实际运行旧 commit（修复前的 estimateEngine.js，第 460 行 SQLite `.run(`）**。本地 b1f0927 已含修复且工作树干净，但线上执行的是修复前代码——Phase 3.1.2 修复本身正确且已提交，问题在部署侧未真正生效（可能 Render 构建缓存 / 部署的不是 b1f0927 / 部分文件未更新）。启动日志 `[dbAsync] PostgreSQL schema ready` 仅证明 ensureCloudSchema 跑过（新旧 dbAsync 都有此日志），**不能证明新 dbAsync/estimateEngine 已上线**。
- 结论排除：B（当前仓库已无 SQLite fund_estimate 写入）=否；C（dbAsync.run 云上 fallback SQLite）=否（isCloud 恒 true）；D（第二未发现的 SQLite fund_estimate 写入）=否（全仓扫描仅旧版 :445-460 一处）。
- 行动建议（不代执行）：在 **Render 控制台核对「Deployed Commit」是否为 b1f0927**；若不是，重新部署/清除构建缓存/确认 push 的是该 commit；然后复测 4 只基金 estimate。
- 红线遵守：纯只读审计，未改代码/环境变量/数据库、未部署、未 DROP 数据、未进 Phase 3.2。

## Phase 3.1.3 生产估值数据质量审计（只读，2026-08-18 实际 API 实测）
- 前提：用户已重新部署使 PG 估值链路正常（本次 4 只基金 /estimate 均成功返回，无 FK 错误，确认 Phase 3.1.2.1 的「旧 commit」问题已通过重部署解决）。
- 对象：022184/018147/014002/016665 四只 QDII。
- **关键代码定义（estimateEngine.js）**：`quote_coverage`(415)=pricedHoldings.length/holdings.length（计数率）；`holdings_weight_coverage`(416)=publishedWeight=Σ全部 top-10 权重（≠已报价权重覆盖，命名有误导）；`holdingsChange`(340)=Σ(priced.change% × weight)（不归一化，直接组合贡献）；`estimate_change`(370-391)：h&s 有限→0.7h+0.3s-cash；仅 h 有限→h-cash；仅 s→s-cash；都无→public/unavailable。
- **016665**：10 持仓权重和=0.2909，与 API holdings_weight_coverage 精确相等 → 该字段=披露 top-10 权重和，仅代表基金约 29% 敞口（QDII 仅披露 top-10，正常）；quote_coverage=0.9=9/10 取到行情。缺失 1 只=**JP3236330001（铠侠，ISIN 格式）**——确定性无法解析（stockSecIds 生成非法 `0.JP3236330001`、getYahooSymbol 返回超长 `JP3236330001`），与 9/10 失败数吻合。沙箱出站被封无法复现 Render 实时解析，但 JP 失败与网络无关。estimate=-0.000029（≈-0.003%）正确（仅 25% priced 权重）。
- **018147**：sector_change=null/benchmark=null 因 `fundSectorMap`/`nameRules` 无「新兴市场」映射 → 正确退化为 holdings-only（estimate=holdings_change=0.012441 ✓）。note quote_coverage=0.8=8/10；结构可疑缺失候选 000660（误存三星代码，解析为无效 `0.000660`）。
- **022184**：fundSectorMap[022184]=technology → benchmark=515000；estimate=0.7*(-0.00353)+0.3*(-0.0102)=-0.005531 ✓。quote_coverage=0.9，缺失 1 只疑为 **285A**（非标准字母数字码，结构无法解析，同 JP）。
- **014002**：名称「智能科技」命中 nameRules /智能/ → technology；quote_coverage=1.0（10/10 全标准美股码）；estimate=0.7*0.022925+0.3*(-0.0102)=0.012987 ✓。
- **行情源判断（P1-3）**：沙箱到 Eastmoney/Yahoo 出站被封（fetch failed），无法直接确认 Render 出口；但生产覆盖呈选择性（014002 100%、其余仅结构性坏码缺失），与「Eastmoney 大面积故障」不符 → 缺失主因为**持仓代码格式问题**（ISIN/非标码），非行情源中断。
- 分级：P0=无（均属正常/数据格式）；P1=① 持仓代码格式（JP3236330001/285A/000660 等）→ 取不到行情 ② 018147/016665 缺 sector 映射 → 估值缺行业因子；正常现象=holdings_weight_coverage 低（QDII top-10 仅 29%~70% 正常）、estimate 退化为 holdings-only（设计预期）。
- 红线：纯只读，未改代码/库/算法/行情源/API/配置，未进 Phase 3.2。

## Phase 3.2 生产估值数据质量修复（已 commit，待 push 部署）

目标：达「估值功能可用」。只修 Phase 3.1.3 的 P1（不改公式/库/calibration/行情源/API）。

修复内容（commit 898165e，main）：
1. **P1-1 持仓代码归一化**：`marketService.js` 新增 `STOCK_CODE_NORMALIZATION` + `normalizeStockCode` + `toYahooSymbol` + `fetchStockQuoteYahoo`；`fetchStockQuote` 先归一化、东京(.T)/韩国(.KS)跳 Eastmoney 直接走 Yahoo（避免误查同名 A股）、其余市场 Eastmoney 优先+Yahoo 兜底。关键映射（均先 web 查证）：
   - `JP3236330001`(ISIN)= Kioxia/铠侠 东京 285A → `285A.T`
   - `285A`= 同 → `285A.T`
   - `000660`= SK Hynix/SK海力士 KRX → `000660.KS`（注：000660 曾为深市 *ST南华 2004 已终止，本应用仅 QDII 外股出现）
2. **estimateEngine.js**：`getYahooSymbol` 委托 `toYahooSymbol(normalizeStockCode())`（旧版对 ISIN/285A 生成无效 Yahoo 码）；`fetchHistoricalChange` 归一化并跳过 .T/.KS 的 Eastmoney 循环（QDII 估值走此历史路径）。
3. **P1-2 sector 映射**：`estimateConfig.js` `fundSectorMap` 增 `018147→broad`(沪深300 510300)、`016665→technology`(科技 515000)，用项目已有可抓 ETF。

验证：node --check 三文件 OK；归一化单测（sandbox 直跑 marketService 函数）全 PASS；全仓 fund_estimate 读写仍走 dbAsync（无新增 SQLite 云端写入）；`node --test` 4 文件仍全 FAIL=既有环境问题（缺 pg/qrcode，非本次引入）。

待办：用户在 GitHub Desktop push → Render 自动部署；部署后只读验证 4 只基金 /estimate（022184/018147/014002/016665）+ /api/health + Deployed Commit 898165e；对比修复前后 quote_coverage/estimate_change/benchmark/sector_change。

注意：render 推不动（agent 无凭据）；Eastmoney 出口/Render 本地引擎行情属暂缓项，本阶段未动。不进 Phase 3.3。

## Phase 3.2 生产估值验收 + Phase 3.2.0 密钥修复（只读，2026-08-18 实测）

- **Render 只读验收结果（GET 公开 API）**：/api/health success=true、database=postgres、server running。4 只基金 /estimate 全部 success=true：quote_coverage 均=1.0（022184 0.9→1.0、018147 0.8→1.0、016665 0.9→1.0、014002 1.0）、fallback=null。018147 现出 benchmark 510300(broad)、016665 现出 benchmark 515000(technology)（=898165e 行为证据，确认 898165e 已部署）。
- **重点验证全过**：016665 的 JP3236330001/285A → Yahoo 285A.T 恢复（qc 1.0）；018147 的 000660 → Yahoo 000660.KS 恢复（qc 1.0）；018147+016665 均出现 benchmark；Yahoo 行情出口在 Render 可用；4 只全 success；无结构性行情缺失。
- **Phase 3.2 估值验收结论 = A（已达生产可用）**：公式/架构未动、无回归（014002 三指标与修复前完全一致）。
- **Phase 3.2.0 密钥修复 = 受阻（agent 无 Render 面板/API 权限）**：SOURCE_SECRET_KEY 仅用于 crypto.js AES 加密落库 source_credentials（provider 同步凭证），与 /estimate 读路径无关（故估值验收 A 不受其影响）。禁用的 dev 默认密钥 = `genius-trader-dev-only-source-secret`（crypto.js DEV_FALLBACK_KEY）。agent 无法读/写 Render 环境变量、无法触发 redeploy、无法读部署日志。该步骤必须由用户在 Render 面板手动完成：Environment 设 SOURCE_SECRET_KEY=原生产历史值（绝不重新生成、绝不用 dev 默认）、Save、Manual Deploy；部署后查日志不再出现 "[source-credentials] 未设置环境变量 SOURCE_SECRET_KEY"、确认 commit 仍 898165e、health 仍 postgres。明文密钥不得出现在聊天/日志/报告。
- 红线遵守：未 sync/import/calibrate/重算/改库/改环境变量/改代码。

### Phase 3.2.0 密钥来源只读排查补充（render.yaml 关键发现）
- `render.yaml:15-16`：`SOURCE_SECRET_KEY / generateValue: true` → Render Blueprint 部署时**自动生成随机生产密钥**并存于 Render 控制台 Environment，仓库无明文。全仓搜索仅 README/render.yaml(声明)/crypto.js/sourceCredentials.js 引用该名；其他密钥名（SOURCE_SECRET/SOURCE_CREDENTIALS_KEY/SOURCE_ENCRYPTION_KEY/ENCRYPTION_KEY）全仓零命中。
- 结论：密钥值只在 Render 面板、不在项目/配置；生产若走 Blueprint 则该键已自动设置（非 dev 默认），可能根本无密钥不一致问题。
- `source_credentials` PG 表结构确认存在（dbAsync.js:192）：id/user_id/source_name/token/refresh_token/cookie/user_info/status/created_at/updated_at，UNIQUE(user_id,source_name)。但本沙箱无 DATABASE_URL、无 PG 连接权限，**无法确认是否有历史加密行**。
- 判断 = D（无法确认→不修改、不生成新密钥）。下一步唯一操作：用户在 Render 面板核对 SOURCE_SECRET_KEY 是否已存在（存在即保持、勿 regenerate；仅当确为空才从原值填入），不输出明文。
- **只读 COUNT 路径实测（2026-08-18）**：本沙箱无 PG 凭据，唯一生产只读 API = `GET /api/debug/database`（fund.js:73，DEBUG_KEY 鉴权，仅返回 source_credentials.count + 每行 has_credential 布尔，绝不返回 token 原文）。keyless 调用返回 401 `DEBUG_KEY 缺失或不正确` → 需 DEBUG_KEY 才能取数；Render Shell 同样无权限。结论：source_credentials 总记录数/含密文数**仍无法确认**（环境无法直连 PG）。下一步二选一：①用户给 DEBUG_KEY（仅调试密钥，非生产 secret）→ 我调只读接口取 count；②用户在 Render Shell/psql 跑 `SELECT COUNT(*) FROM source_credentials` 与含密文 COUNT，回传数字（不回传 token）。count=0→可生成新生产密钥；>0→禁止生成（且若现值用 dev 默认加密，新密钥会使旧行不可解密，需重授权 provider）。

- **DEBUG_KEY 存在性只读排查（Phase 3.2.0，2026-08-18）**：`fund.js:74` `process.env.DEBUG_KEY || ''`（无硬编码默认）；`render.yaml` 仅声明 NODE_VERSION / SOURCE_SECRET_KEY(generateValue) / DATABASE_URL，**未声明 DEBUG_KEY** → 非 Blueprint 自动生成，若存在只能手动加在 Render Environment。本沙箱无 Render API 权限，且 keyless GET 401 在「未设」与「已设但 key 错」两种情形完全一致、无法区分 → **无法从本环境确认 DEBUG_KEY 是否已设置**。未输出 DEBUG_KEY、未生成 SOURCE_SECRET_KEY、未改库/代码/环境变量。下一步唯一操作：用户在 Render Environment 页面核对 DEBUG_KEY（有则给值让我跑只读接口；无则走 psql + DATABASE_URL 跑 COUNT，Free 实例无 Shell）。

- **Phase 3.2.0-B 临时只读诊断接口（2026-08-18，已实现未提交）**：新增 `GET /api/debug/credentials-status`（fund.js，仅 GET，DEBUG_KEY 鉴权；DEBUG_KEY 未设则运行时生成一次性随机令牌 `__diagToken` 仅打印到服务端日志一次、非永久密钥、不进 API）。仅返回 `{success, source_credentials:{count, credential_count}}`；count=`SELECT COUNT(*) FROM source_credentials`，credential_count=同表 WHERE `token<>'' OR refresh_token<>'' OR cookie<>''`。**关键 schema 修正**：source_credentials.token 为 `TEXT NOT NULL DEFAULT ''`，故 `token IS NOT NULL` 恒真，必须用 `<>''` 才正确反映"存在加密凭证"（与现有 debug 接口 `Boolean(r.token)` 一致）；按用户字面 SQL 会令 credential_count 恒等于 count 失真。走现有 dbAsync.get → PG(isCloud)。`node --check` 通过；`git diff` 仅 +38 行 0 删（无既有逻辑改动）。未提交/未 push/未部署（按指令待报告）。

- **Phase 3.2.0-C 加密密钥兼容性只读验证（2026-08-18，未执行实证）**：用户提供前提=生产 source_credentials count=6/credential_count=6；当前 Render 未设 SOURCE_SECRET_KEY→crypto.js 走 DEV_FALLBACK_KEY(`genius-trader-dev-only-source-secret`)。目标实证 6 条是否可用 DEV_FALLBACK_KEY 解密。本沙箱**无 DATABASE_URL/PG 凭据/Render Shell(Free)**，且本阶段禁改代码（不能加返回密文的端点）→ **无法在沙箱执行实证解密测试**。逻辑推断（非实证，倾向 A）：SOURCE_SECRET_KEY 从未在 Render 设置（日志"未设置"），6 行几乎必然用 DEV_FALLBACK_KEY 加密→可解密；但若历史上曾设过又被清空则可能为 C，必须实证。安全路径：独立只读 Node 脚本（require 项目 crypto.js.decryptText + pg，**运行前 `delete process.env.SOURCE_SECRET_KEY` 复现生产路径**，SELECT token/refresh_token/cookie，逐条 try decrypt，只计 encrypted/decryptable，绝不打印明文，结束 `pool.end()`），由用户持 DATABASE_URL 本地跑或授权我持只读连接串跑（pg 已在 node_modules）。

- **Phase 3.3 estimate_change 偶发/历史为 null 根因与最小修复（2026-08-18）**：根因=Render 出网屏蔽东方财富。fetchHistoricalChange 主源 push2his 东方财富 K 线在 Render 必失败（已有 Yahoo 兜底，故持仓/行业历史行情通常可取；4 只基金当前 trade_date=2026-08-17 历史路径仍非 null）。但最后兜底 `getRealtimeFundEstimate` 主源是 fundgz/东方财富（marketService.js:129/156），在 Render 被封→回退本地 NAV 历史（source='本地数据库缓存'），而 estimateEngine.js:373 原代码要求 `source==='fundgz'` 才接受，导致该有效有限值被丢弃→`estimate_change=null`。修复（estimateEngine.js，2 处，未改公式/架构/Phase 3.2 行为）：① 放宽最后兜底接受任何有限值（fallback 标记 `public-estimate-local`）；② fetchHistoricalChange 的 Yahoo Fallback 1 增加 `query2.finance.yahoo.com` 主机冗余（指令 F）。新增 `server/tests/estimateFallback.test.js`（mock 出网屏蔽，断言 estimate_change 由本地 NAV 兜底为有限值而非 null），`node --test` 通过；`node --check` 通过。未提交/未 push/未部署（待用户在 GitHub Desktop 推送，部署后我复验 4 只基金 estimate_change 无回归）。

- **Phase 3.3 提交前检查 + Phase 3.4 全量巡检基线（2026-08-18，只读）**：git status 确认未提交改动 = package-lock.json(既有 npm install) + server/api/fund.js(Phase 3.2.0-B 既有) + server/services/estimateEngine.js(本阶段) + 未跟踪 server/tests/estimateFallback.test.js(本阶段) + 未跟踪 cred_audit.js(Phase 3.2.0-C 脚本，勿提交)。Phase 3.3 实际改动仅 2 文件：estimateEngine.js + estimateFallback.test.js。`node --check estimateEngine.js` OK；`node --test estimateFallback.test.js` = 1 passed/0 failed。agent 无 GitHub 凭据→由用户 GitHub Desktop 提交这 2 文件（msg: `fix: add estimate fallback for blocked market sources`）并 push main 触发 Render；未动 fund.js/package-lock.json/库/数据。
- **Phase 3.4 生产全量巡检基线（当前 deploy=898165e，未含 3.3 修复）**：56 只基金全部 success=true。**estimate_change 非 null=30、null=26**；fallback 分布 unavailable=26 / sector-only=18 / None=12；quote_coverage<1 的 43 只；sector_change=null 的 34 只（含 unavailable 26 + holdings-only NaN 8）。26 只 estimate_change=null 即 Phase 3.3 修复目标（两者皆不可得→最后兜底本地 NAV 被旧代码拒收）；部署 3.3 后这些有 NAV 历史的基金应转非 null。4 只重点基金（fallback=None、quote_coverage=1）值：022184=0.014792、018147=0.007063、014002=0.014727、016665=0.003139，均非 null 且 3.3 不动其路径→无回归。部署后需重跑本巡检确认 26→减少。

## Phase 3.5-B 生产估值 Coverage 一致性只读核验（只读，未改代码/库/部署）
- 目标：解释 Phase 3.4 的 `quote_coverage<1 = 43` 为何与 Phase 3.5 的 `56/56 实时覆盖率=100% / missing=0` 不同。
- **quote_coverage 计算位置**：`estimateEngine.js:414` = `holdings.length ? pricedHoldings.length/holdings.length : 0`。分母=latestHoldings（前十大 `LIMIT 10` 最新 report_date）；分子=quoteResults.filter(Boolean)。只统计前十大，不按权重过滤，美股未开盘 change_percent=0 仍计入。
- **两条路径不同（关键）**：`/stock/:code`→`fetchStockQuote`（纯实时、无缓存）；`/estimate`→`quoteFor`（先查 stock_price 缓存再 fetchStockQuote；QDII 且 targetDate≠今天走 `fetchHistoricalChange` 历史日期路径）。两者实时分支同收敛到 `fetchStockQuote`（Eastmoney→Yahoo），差异在缓存层+历史/实时日期分支+测量时点。
- **三大差异根因（均非证券代码映射问题，Phase 3.5 已实证 56/56 实时可取）**：
  ① 空持仓口径：`/estimate` 无持仓→coverage=0（计入43）；Phase 3.5 脚本 0/0=1.0（用户实测"前十大=0/成功=0/缺失=0/coverage=1.0"）。
  ② QDII 历史交易日 vs 实时："历史某交易日覆盖率"与"实时覆盖率"是两个不同指标；Render 屏蔽 Eastmoney 致历史 K 线部分缺失→estimate<1，实时全成功→realtime=1。
  ③ 测量时点出网/缓存冷状态：Phase 3.4 在 deploy=898165e（未含3.3）Eastmoney 屏蔽窗口测得；Phase 3.5 更晚 Yahoo 可达、纯实时复测得全 1。
- **结论 = A（不需要修改代码）**：差异属统计口径+测量时点网络状态，非缺陷；Phase 3.5 实时 100% 为权威当前结论，可验收。可选（非必需、暂不执行）：若要对齐口径，仅可将空持仓 `:0` 改 `:1`（estimateEngine.js:414）。日股/韩股映射在 898165e 已修，禁改 marketService.js。
- 数据可得性：本机 Phase 3.4 基线仅存聚合值（43/<1、26 null 等），未存逐只明细，故无法从磁盘恢复 43 只逐只值；26 只 estimate_change=null 名单为用户给定（与 43 重叠子集）。
- 红线遵守：未调用 /estimate（其写 fund_estimate + stock_price 缓存）、未改代码/库/环境变量/部署、未输出 secret。报告：`.workbuddy/Phase3.5B_coverage_analysis.md`。

## Phase 3.6 生产数据完整性 + 小程序核心数据链 验收（实测闭环，结论 B）
- 目标：回答「后端数据能否放心交给小程序使用」。结论=**B（存在问题但不阻塞，附上线前硬门禁）**；总进度 88%（架构+契约 100%、mp1 待联调）｜⚠️ 数据就绪度实测 25%（14/57 可用）｜目标：小程序可用。
- **连通性恢复**：续做时 `curl /api/health` 返实时 JSON，确认沙箱可直连生产；故不再依赖用户本地跑脚本，本环境实跑 `phase36_health.py` 闭环（两次运行一致）。
- **核心契约映射（代码实证，全匹配）**：mp1 硬依赖 = `/api/fund/:code`(fund_name/latest_nav/history/holdings) + `/api/stock/:code`(quote.change_percent)。`getFund`/`getFundHoldings`/`/api/stock/:code` 返回字段与 mp1 读取逐一吻合。
- **决定性发现①**：mp1 默认 `local` 源不调 `/estimate` → Phase 3.4 的 `estimate_change=null`(26)/`quote_coverage<1`(43) 仅影响 opt-in 第三方源，默认体验零影响。
- **决定性发现②（实测坐实）**：57 只基金逐只核验 → **仅 14 只有前十大持仓、19 只有 NAV/历史、38 只完全空白**。复检 001595/002207 在 `fast=1` 异步 import 触发后仍空白 → 按需导入当前环境不补全，与 Phase 3.3「Render 出网屏蔽 Eastmoney」一致。数据健康分级：🟢完美13 / 🟡仅净值曲线(无持仓)5 / 🟠过期1(019333,622天) / 🔴完全空白38。实时股票行情 0 缺失（140 条持仓行情 100% 可取）。
- **红线副作用披露**：`fast=1`(fund.js:721) 会异步触发 `importFund`（DB 写尝试），非纯读；此外 fund.js:708 `getFund` 空时自动 import。实测零数据改动（复检空白基金仍空）。未调 `/estimate`、未改代码/库/环境变量/部署、未输出 secret。
- **判定 B 理由**：契约全匹配+优雅降级+无崩溃路径 → 开发不被阻塞；但 43/57 无可用数据，真实上线前须补齐导入(或确认无人持有) → 故非 A(43 空白)、非 C(开发不阻塞)。
- 交付：`phase36_health.py`（Temp，纯 GET）；逐只表 `.workbuddy/Phase3.6_health_raw.md`（57 行+异常清单）；报告 `.workbuddy/Phase3.6_production_health.md`（含红线核对附录）。
- 红线遵守：未调 /estimate、未改代码/库/环境变量/部署、未输出 secret；`fast=1` 写副作用已披露且实测零影响。

## Phase 3.6-B 生产用户持仓 × 基金数据缺口 关联核验（只读，结论=数据上线门槛未达成）
- 目标：把 Phase 3.6 的 43 只缺口基金与真实用户持仓 JOIN，确定哪些必须优先修复。结论=**真实持仓用户数据缺口命中，上线门槛未达成（非 A 非纯 B，属「开发可继续但上线须先修复」）**。
- **账户覆盖（用用户提供的 天才/李总 token 静默调 GET /api/account/state，token 仅入请求头、未回显/落盘/写报告）**：
  - 天才(user1)=主账户 **0 基金**；guest(0)=主账户 **0 基金**。
  - 李总(user2)=`小倍养基-默认账户`（**sync 账户，来源小倍养基**）**36 只基金** ← 唯一有真实持仓的 protected 用户。
  - 稳健组合/成长组合(user3)=**未提供 token，本阶段未核验（缺口）**。
- **JOIN 结果（36 只已核验持仓）**：A 真实持仓+完整=8；**B 真实持仓+完全缺失(EMPTY)=26**；B2 真实持仓+有净值无前十大(NOHOLD)=2；D 真实持仓+过期=0（019333 过期基金未被任何已核验用户持有）。
- **金额分布（关键）**：完整 A=¥19,621(37%)；**完全缺失 B=¥32,026(61%)**；B2=¥1,058(2%)。→ 李总持仓金额 61% 落在无数据基金上。
- **关键结论**：当前生产数据**不满足**「用户真实持仓可在小程序正常使用」上线门槛——mp1 经 `/api/fund/:code` 读这 26 只将得空白/零值。按用户四分类，26 只属 B（上线前必须修复）。
- **重要缓解/待验证点**：李总账户是 sync 账户，mp1 对 sync 账户可能直接走 provider 同步快照（含净值/收益）而非后端 fund 表 → 26 只缺口在 mp1 实际渲染中或可被 provider 数据覆盖。需 **Phase 3.6-C** 验证 mp1 对 sync/provider 账户的取值路径（不在本只读阶段）。
- **未核验缺口**：user3（稳健/成长）未给 token，其持仓可能再增 B 数量；建议补 token 闭环。
- 红线遵守：仅 GET /api/account/state（纯读 user_data，不触发 importFund）；**未调任何 /api/fund/:code**（复用 Phase 3.6 健康表，零 import 风险）；未调 /estimate；未 INSERT/UPDATE/DELETE；未改代码/环境变量/Render 配置；token 未输出。
- 交付：报告 `.workbuddy/Phase3.6B_holding_join.md`（四分类明细+金额+严重度+结论+总进度 88% 含数据门槛未达成标注）。

### Phase 3.6-B 修正（token 失效 → 天才2.0 未核验）
- **关键修正**：上一轮「天才(user1) 主账户 0 基金」经本次复核实为 **guest(0) 回退数据**，非天才本人。证据：`GET /api/auth/me` 对 `f58cce50…`（用户标为天才的 token）返回 `user:null`（无效/过期）；`/api/account/state` 静默回退 guest→主账户 0 基金。李总 token `8428defb…` 经 `/api/auth/me` 解析为 **id=2 / lizong@163.com**（有效），36 基金核验成立。
- **天才2.0（预期 17 只）因 token 失效无法读取** → BLOCKED，未伪造。报告已撤回上一轮对天才的误判。
- **已核验结论（仅李总 36 只）**：A=8/¥19,621、B=26/¥32,026、B2=2/¥1,058、D=0；019333 未被李总持有。真实缺口 28/36（63% 金额）。仍不满足上线门槛；天才侧缺口或被低估。
- 就绪脚本 `C:\Users\Administrator\AppData\Local\Temp\phase36B_consolidate.py`：用户提供有效天才 token 后一键合并两账户去重 + 四分类 + 019333 核验。
- 红线遵守：仅 GET /api/account/state + /api/auth/me；未调 /api/fund/:code；未调 /estimate；未改代码/库/配置；token 未输出。

### Phase 3.6-B 修正完成版（有效天才 token 已补）
- 用户提供有效天才 token `f639986…`：`/api/auth/me` → **id=1**，账户 `天才2.0` **17 只基金**确认。与上轮失效 token 的 guest 误判彻底区分。
- **合并 JOIN（天才2.0 17 + 李总 36，去重）**：合计去重 **46 只**。A=12/¥129,953、B=29/¥106,499、B2=5/¥183,861、D=0。
- **缺口合计 34 只（B=29 完全缺失 + B2=5 缺前十大），占合并持仓金额 69%**（¥290,360/¥420,313）。
- **两账户同时持有 7 只**：002207 / 013309 / 014002 / 016665 / 018147 / 019633 / 022184（去重时账户维度保留）。
- **019333（过期622天）未被任一账户持有** → 不升 D。
- 分类明细：天才2.0 A=8/¥128,630、B=4/¥76,612、B2=5/¥183,861；李总 A=8/¥55,392、B=26/¥34,427、B2=2/¥17,279。
- **结论**：当前生产数据【仍不满足】上线门槛——缺口直接命中两位真实用户；天才2.0 为 local 硬缺口，李总 sync 账户或可被 provider 快照缓解（待 Phase 3.6-C 验证）。剩余未核验=user3（稳健/成长）无 token。
- 最终报告 `Phase3.6B_holding_join.md` 已覆盖写入。总进度 88%｜目标小程序可用｜数据门槛未达成。

### Phase 3.6-C sync 账户真实取数路径验收（只读代码）
- **目标**：确认李总 sync 账户「小倍养基-默认账户」(36 只, 其中 26 只 B 类) 在 mp1 是否真走 provider snapshot 绕过 B 类缺口。
- **核心链路（代码实证）**：
  - 账户恢复：`app.js:341 loadStateFromCloud` → `GET /api/account/state` → `accounts[].funds[]`(=provider snapshot，含 code/name/amount/holdingProfit/today/hold/transactions，**不含 NAV history/holdings**)。
  - 列表：`portfolio.js:255/544` 直接读 snapshot 渲染 名称/金额/持有收益/今日%(today)/收益率；`?fast=1`(L621) 仅取徽章(装饰)，B 类走灰色兜底。
  - 详情：`fundDetail.js:112 initDetail` 两层——①`loadFundLocalDetails`(L122) 读 snapshot(名称/金额/收益/今日/交易) 不经 /api/fund/:code；②`fetchFundServerDetails`(L167) 调 `/api/fund/:code?refresh=1&fast=1` 取 history(曲线/业绩/当日净值)+holdings(前十大)；`_enrichHoldingsQuotes`(L226) 仅当 holdings 非空才调 `/api/stock/:code`。
  - **默认 local 源不调 /estimate**（L433 仅 yjb/xbyj opt-in）；详情不调 /estimate，仅 `/calibration`(只读)。
- **关键发现**：`utils/mockHistory.js` 的 `generateMockHistory`(按 code 生成假 NAV 曲线) **已定义但无任何 import 调用** → B 类详情显示真实空态（"暂未同步到历史净值"），**不造假**。
- **26 只 B 类逐项**：列表(名称/金额/收益/今日%) ✅完整(快照)；详情头部 KPI ✅；当日净值/历史曲线/前十大持仓/持仓股票涨幅 ❌缺失(空态)；不崩溃、不假数据。
- **最终判定 = C**：列表可用但详情页受影响，需要修复详情数据链。B 类从「硬阻塞」降级为「列表可用+详情部分降级」。修复路径：①importFund 补 history+holdings(真实) ②扩展 sync 导入写 history/holdings ③不接 mockHistory(造假风险)。
- 红线遵守：纯静态读码，未调任何生产接口、未触 importFund、未调 /estimate、未改代码/库/部署、未用 token。
- 报告 `Phase3.6C_sync_path.md` 已生成。总进度 88%｜目标小程序可用｜数据门槛：列表可达标、详情需修复(C类)。

### Phase 3.7-A 导入链只读诊断（判定 A）
- **目标**：诊断李总 26 只 B 类缺口根因，判定数据源是否可恢复。只读、未改代码/库、未调 import/refresh/fast/estimate。
- **importFund 链路（代码实证）**：`importFund`(fundService.js:344)→`collectFund`(230)→NAV/history=`fetchHistory`(marketService.js:301，主源 fund.eastmoney.com + 备用 api.fund.eastmoney.com，**全是 Eastmoney，无 Yahoo/其他 fallback**)；holdings=`fetchHoldings`(365，fundf10.eastmoney.com jjcc，**仅 Eastmoney**)。→ 基金 NAV/历史/前十大 100% 只依赖 Eastmoney。
- **另一 fund 表写入者**：`portfolioService.upsertFund`(portfolioService.js:13) 只写 fund_code/fund_name/updated_at，**不写 fund_type/nav/holdings**——解释 26 只"有名字无数据"。
- **生产只读实证**：`GET /api/funds` → 57 只全部 7 天内更新、**45 只今天**(000001=09:09,016371=06:43)；`GET /api/fund/000001`(good)=latest_nav 2026-08-17/history 5986/holdings 10 → **Eastmoney 今日可达**；26 只 B 类 plain read 全 `latest_nav=None/history=0/holdings=0` 确认空白；26 只 `updated_at` 全 = 2026-08-18T06:41:10(同批次=sync upsertFund 写入，importFund 从未跑过它们)。
- **判定 = A**：数据源已恢复（Eastmoney 现可达），缺口根因是 importFund 从未为 26 只执行，只需安全补数据；严禁 mockHistory。QDII(012920)/商品LOF(161226) 代表均可取得(161226 期货持仓 jjcc 或偏少，次要风险)。
- **下一步(Phase 3.7-B 实施)**：服务端脚本循环 `importFund(code,{force:true})` 26 只(限流 300-500ms)，执行后重跑 Phase 3.6 health + C 验证。
- 报告 `Phase3.7A_import_diagnosis.md` 已生成。总进度 88%｜数据门槛从「未达成」降级为「A 档：待一次安全补数据」。

### Phase 3.7-B 26只真实基金数据补全脚本（生成 + dry-run，禁止执行 importFund）
- **目标**：补齐天才2.0+李总真实持仓中 26 只 B 类（NAV/history/holdings 三项全缺）基金，基于 Phase 3.7-A 判定 A（Eastmoney 已恢复，importFund 正确入口）。
- **26 只代码来源**：直接取 Phase 3.7-A §5 已确认清单（并经 Phase 3.6 健康表逐只复核为三项全缺），不猜测：
  `002207 006503 008702 010736 011452 011949 012920 012922 016708 017731 017811 017994 018168 018178 019889 020412 021842 024203 024239 025422 025500 025833 026211 161226 501205 539002`。
- **脚本**：`server/scripts/phase37b_import_missing_funds.js`（新建，未修改任何业务代码；参照 `seedFunds.js` 写法，`require('../services/fundService')` + `require('../database/db')`，继承后端 DB 目标——Render 走 PG(DATABASE_URL)、本地回退 SQLite，无需改环境变量）。
- **关键设计（守红线）**：
  - 默认运行 = DRY RUN：脚本**动态 require** 业务模块仅发生在 `--execute` 分支，dry-run 完全不加载 fundService/DB 链 → 默认零副作用。
  - 仅 `--execute` 才调 `importFund(code,{force:true})`；`--resume` 读 `phase37b_result.json` 跳过已成功、续跑失败（支持中断恢复）。
  - 执行前硬校验：数量==26 / 无重复 / 全 6 位 / 无空串 / 无未知代码；任一失败立即终止，不调 import。
  - 单只失败不中断整体；单只重试最多 2 次 + 超时(默认 60s) + 基金间限流(默认 500ms)。
  - 执行后**只读校验**（getFund + fund_nav COUNT + getFundHoldings）生成 `Phase3.7B_result.md`：PASS/NAV+历史 ok 但前十大缺失→PARTIAL(如 161226 期货LOF，绝不伪造)/FAIL。
  - 结果仅写本地 `phase37b_result.json` / `Phase3.7B_result.md`，不写生产库。
- **验收**：`node --check` SYNTAX_OK；DRY RUN 输出符合规定格式（清单+总数26+预计动作+数据库写入YES+未执行声明）。
- **本阶段状态**：脚本已生成、dry-run 通过、**尚未执行 importFund、生产库未修改**。总进度 **90%**｜目标：小程序可用。
- **下一步（待用户确认才执行）**：用户在后端环境跑 `node server/scripts/phase37b_import_missing_funds.js --execute`（需 DATABASE_URL/PG 可达、Eastmoney 出网），跑完自动出 Phase3.7B_result.md；再重跑 Phase 3.6 health + Phase 3.6-C 验证 26/26。若 26/26 PASS → 94%。
- 红线遵守：未执行 importFund、未调 refresh/fast/estimate、未改业务代码/库/结构/环境变量、未部署、未改 GitHub、未造 mock 数据。

### Phase 3.7-B-Execute（执行前安全门禁触发 → STOP，未执行）
- **用户授权进入真实执行阶段**：`node server/scripts/phase37b_import_missing_funds.js --execute`。执行前按用户硬规则做最后确认 + DB 目标校验。
- **26 只硬校验（静态，无 DB/无 import）全部 PASS**：数量=26、全部 6 位、无空串、无重复、无未知代码（与 TARGET_CODES 完全一致）。
- **🔴 关键安全门禁触发**：本环境 `process.env.DATABASE_URL` = **UNSET**。`dbAsync.isCloud()=Boolean(DATABASE_URL)` → 为 false → 脚本将回退写 **本地 SQLite**（`server/data/portfolio.sqlite`，当前不存在），**而非生产 Render PostgreSQL**。后端 `process.env.DATABASE_URL` 直接读取、无 dotenv、仓库仅有 `.env.example`（模板，无真实值）→ 依赖 Render 注入。
- **pg 驱动已装**（`node_modules/pg` 存在）→ 若提供生产 DATABASE_URL，PostgreSQL 路径可正常走。
- **用户硬规则 §三**：「如果检测到 SQLite 而当前目标应该是生产 PostgreSQL：立即停止，不执行。」→ 严格执行 **STOP**。**未执行 importFund、未写入任何数据库、未设置/修改环境变量、未部署、未改代码**。本地 SQLite 写是错误目标（mp1 消费的 Render PG 仍空白，属无效写入，且违反用户明示）。
- **agent 无生产 DATABASE_URL 值**（属 Render 控制台 secret，不在仓库/本地）→ 无法自行设置。
- **可执行路径（待用户选一）**：
  - A：用户给出生产 `DATABASE_URL`（Render External URL），以一次性环境变量运行本脚本（仅运行期传入、不 commit、不写入持久环境/文件）。
  - B：在 Render 服务自身运行脚本（其环境已注入 DATABASE_URL）→ 直接写生产 PG。
  - C：经生产服务端 HTTP 触发 importFund（生产服务端带 DATABASE_URL，写 PG 正确）；但需用户**显式豁免**此前 refresh=1/fast=1 限制（Phase 3.7-B 红线与本章 §五验收都禁 refresh/fast/estimate），否则不采用。
- **当前状态**：脚本已生成 + dry-run 通过 + 硬校验通过 + DB 目标校验发现本地 SQLite 陷阱 → 按用户规则 STOP，等用户决定连接生产 PG 的方式。总进度维持 **90%**｜目标：小程序可用｜数据门槛：A 档待安全补数据（等待生产 DB 可达）。
- 红线遵守：未执行 importFund、未触发任何写接口、未改代码/库/结构/环境变量/Render 配置、未部署、未造 mock 数据、未输出 secret。

### Phase 3.7-C 系统基金基础数据覆盖机制（只读架构核验，结论=需建公共同步机制）
- **用户决策（架构层）**：暂停 Phase 3.7-B `--execute`，禁止按「天才2.0+李总持仓」补 26 只。理由：`fund / fund_nav / fund_holdings` 是**公共基金基础数据层**，完整性不应由「某用户是否持有」决定；未来用户加仓、新用户买目录外基金、新 sync 都会复现缺口。→ 应建「基金基础数据同步机制」而非「用户持仓补数据机制」。
- **只读目录核验（GET /api/funds + GET /api/fund/:code 无 refresh/fast，实测零写入；57 只均已 upsertFund 预建行故不触发自动 import）**：
  - 目录总数 **57**：A 完整=**13(22%)** / B 三项全缺=**38(66%)** / C 无history=0 / D 有NAV+history无holdings=**5(9%)** / E 过期(019333,NAV停2024-12-04)=**1**。
  - 至少缺一项 = B(38)+D(5)=**43 只**；计入过期 E 则 **44 只(77%)** 基础数据不可用。
  - **关键实证**：38 只 B 类 `updated_at` 全 = `2026-08-18T06:41:10`（sync `upsertFund` 批次时间戳），13 只 A 类 `updated_at` 各异且有 history → sync 导入只建名字行、importFund 从未跑。
- **新增基金机制（静态代码）**：
  - `portfolioService.js:19 upsertFund` 仅 `INSERT fund(fund_code,fund_name,...)`；唯一调用点 `replaceSyncedAccount`(sync 导入, L41) → 每持仓基金建「名字行」但**不调 importFund**。
  - `fund.js:704-713` `getFund` 仅当**无行**才 `importFund` 自动补全 → 名字行已存在被绕过（保护失效）。
  - 手动账户持仓存浏览器 localStorage（`portfolioService.js:4-5` 注释），**不入服务端 fund 表**；仅 sync 账户经 replaceSyncedAccount 写服务端。
  - importFund 触发点枚举：启动 seed(index.js:74)、getFund 无行(fund.js:708)、`?refresh=1`/`?fast=1`(fund.js:721/725)、`/import/:code`(fund.js:556)、估算兜底(estimateEngine.js:269)、批量净值同步(navService.js:37)。**upsertFund/replaceSyncedAccount 本身不调 importFund**。
  - 六问结论：① fund 表自动建（仅名字行）② fund_name 自动写 ③ importFund **不**自动触发(add/sync 路径) ④ fund_nav **不**自动填 ⑤ fund_holdings **不**自动填 ⑥ **存在「只有名字无数据」且正是根因**。
- **推荐修复（待用户下一阶段确认才实施，本阶段未改代码）**：① `upsertFund` 新建行即 `importFund`（写入即补全）；② 复用 `navService.js:37` 批量能力做**定时对账扫描**（系统级同步机制）；③ 保留 `getFund` 懒加载兜底。该机制上线后目录内所有基金（含未来新增）自动补齐，mp1 详情页直接读真实 NAV/历史/前十大，无需按用户补、不碰 mockHistory。
- 交付：`.workbuddy/Phase3.7C_fund_catalog_health.md`（分类汇总+逐只明细+静态分析+六问答+架构建议）。总进度 **92%**｜目标：小程序可用。完成后 STOP 等确认。
- 红线遵守：纯只读 API + 静态读码，未执行 importFund/refresh/fast/estimate、未 INSERT/UPDATE/DELETE、未改代码/库/结构/mp1/环境变量/部署/GitHub。

---

## Phase 3.8 —— PostgreSQL 迁移与低成本方案评估（只读评估，已完成未执行）
- 背景：Render 免费 PostgreSQL 将于 2026-09-07 暂停并进入删除。本阶段纯评估，零写入。
- 架构关键结论：DB 入口 dbAsync.js 由 isCloud()=Boolean(DATABASE_URL) 选 PG(pg Pool) 或 SQLite(node:sqlite 回退)；schema 由 ensureCloudSchema()(dbAsync.js:137, index.js:157 启动自举) 代码生成，无 .sql/migration；SQL 仅 SERIAL/TIMESTAMPTZ/ON CONFLICT/RETURNING/外键，无 JSONB/ARRAY/UUID/扩展 -> PG->PG 仅改 DATABASE_URL，零业务代码/零 schema 改动，mp1 不变。
- 重大发现 schema 漂移：data_sync_state 在 db.js:85(SQLite) 存在，但 ensureCloudSchema(PG 自举) 缺失；fundService.js:212/222 生产 PG 仍引用 -> 新建空 PG+自举会缺表报错。本次靠 pg_dump 带入安全；建议迁移后把该表补进 ensureCloudSchema。
- 1 风险 SOURCE_SECRET_KEY：crypto.js AES-256-GCM(SHA256(key)) 加密 source_credentials 的 token/refresh_token/cookie；render.yaml:15 由 Render generateValue:true 随机生成；.env.example:12-13 明令迁移须沿用原值，否则所有养基宝/小倍同步凭证无法解密。必须在 2026-09-07 前离线保存该密钥真实值（比数据 dump 更紧急）。
- 备份能力：无任何 pg_dump/restore/数据库级备份脚本；accountBackupService.js 仅账户 JSON 快照（仍在同 PG 内），非库级备份。迁移=首次真正备份，须在到期前完成。
- 候选方案对比（价 2026-08 公开估，USD，1~=RMB7.2）：留 Render 付费 PG ~$7/月(零迁移) | Neon Free $0 / Launch ~$1-15/月(推荐) | Supabase Pro $25(不推荐,功能冗余) | Railway $5起(~$10-25) | DigitalOcean 托管 PG $15.15起(稳定生产推荐) | VPS 自建 $5-6(最累)。
- 推荐方案：保留 Render Web 服务不变，仅将其 DATABASE_URL 改指向 Neon Free/Launch；成本 $0-15/月，零代码改动，mp1 不变，工程量极小（仅 1 环境变量）。备选 DigitalOcean $15 长期稳定。
- 数据量估算：整库 < 50MB（57 基金 + 少量用户/账户），pg_dump/restore 秒级。精确行数/库大小由新生成只读脚本 server/scripts/phase38_inspect_db.js(仅 SELECT/元数据,本沙箱无 DATABASE_URL 故未执行) 在生产环境取得。
- 最大风险：1 密钥丢失 -> 同步凭证全灭(比数据更致命)；2 9-7 前未 pg_dump -> 整库永久丢失。回滚：改回 DATABASE_URL 或 restore dump。
- 交付：.workbuddy/Phase3.8_postgres_migration_assessment.md（17 节全）+ 只读脚本。总进度 93% | 目标：小程序可用。完成后 STOP 等确认；下一步(执行迁移)须先保全密钥再按步骤操作。
- 红线遵守：未 INSERT/UPDATE/DELETE/DROP/ALTER、未执行 importFund/refresh/fast/estimate、未改代码/库/结构/环境变量/mp1、未部署、未删 Render PG、未迁移、未输出任何 secret。

---

## Phase 3.9 —— PostgreSQL 迁移前安全准备与容量实测（只读评估，已完成未执行）
- 目标：迁移前准备+评估，零写入。核对并增强 phase38_inspect_db.js（新增预计 dump 大小 = pg_database_size字节 x25%，区间15-40%）；语法检查 SYNTAX_OK；验证无 DATABASE_URL 时安全 no-op（exit 0,不连库）。
- pg_dump/pg_restore 能力：项目内无备份脚本（仅 accountBackupService.js 账户JSON快照,仍在同PG内,非库级/非迁移）；本机/沙箱 PATH 无 pg_dump/pg_restore/psql；项目仅用 pg 驱动 v8.23.0（不含二进制）。结论：执行备份前须先在操作机装 PostgreSQL 客户端。
- 关键澄清：pg_dump 导出的是 source_credentials 的 AES 密文而非明文;解密依赖 SOURCE_SECRET_KEY(不在库内)-> 完整备份 = dump文件 + SOURCE_SECRET_KEY 真实值 两者缺一不可。
- SOURCE_SECRET_KEY 风险确认(不读/不输出/不保存真实值)：crypto.js AES-256-GCM(SHA256(key)) 加密 source_credentials 的 token/refresh_token/cookie; render.yaml:15-16 generateValue:true 由 Render 随机生成; crypto.js 有 DEV_FALLBACK_KEY(仅未设env时启用,生产不应触发),不存在能解密生产密文的备用密钥。密钥丢失=所有同步凭证失效需用户重授权,风险等级最高。
- Neon 2026-08 官方价(USD,1~=RMB7.2)：Free $0(0.5GB存储/100CU-hr月/缩容到0/6h PITR/无信用卡)；Launch 按量无最低($0.106/CU-hr+$0.35/GB月,最高16CU,7d PITR,500GB出网,典型~$15/月)；Scale($0.222/CU-hr,30d PITR,SLA/HIPAA,典型~$701/月)。
- Neon 适配评估：强烈推荐 Free->Launch。完全PG兼容(本项目SQL无JSONB/ARRAY/UUID/扩展);数据<50MB远小于0.5GB;流量低100CU-hr足够;仅改 DATABASE_URL 一处零业务代码;无中国大陆区域(新加坡最近,与Render一致)。
- 迁移前硬前提(用户侧,本阶段未做)：① SOURCE_SECRET_KEY 真实值离线保全(最高优先) ② 装 pg_dump ③ 取得两端连接串 ④ 在Neon建project(本阶段禁止)。
- 是否具备进入正式迁移：方案与工具层面已具备;物理前提(上述用户侧3项)未就绪 -> 方法学就绪/物理未就绪,不得执行迁移。
- 交付：.workbuddy/Phase3.9_migration_preflight.md(十节全:容量待补表/pg能力/密钥风险/Neon价/适配/准备清单/条件/红线/进度) + .workbuddy/Phase3.9_backup_plan.md(五步备份操作手册:pg_dump -Fc/校验/分离保管/回滚/三重保险) + 增强 phase38_inspect_db.js。总进度 94% | 目标：小程序可用。完成后 STOP 等确认。
- 红线遵守：未执行 pg_dump/restore、未创建 Neon、未改 DATABASE_URL/环境变量/代码/库/mp1、未部署、未迁移、未改 GitHub、未读/输/存任何 secret。


## Phase 3.9 跟进 — PostgreSQL 客户端三件套就绪
- 用户截图显示 EDB Stack Builder 中 PostgreSQL 18 v18.6-1「已安装」；经定位确认二进制在 `C://Program Files\PostgreSQL\18\bin`。
- 已将该目录加入用户 PATH（注册表），后续新 CMD 窗口可直接运行裸命令。
- 三个版本号已验证：
  - pg_dump (PostgreSQL) 18.6
  - pg_restore (PostgreSQL) 18.6
  - psql (PostgreSQL) 18.6
- 未处理 SOURCE_SECRET_KEY（用户侧）、未创建 Neon、未改 DATABASE_URL/Render、未执行 pg_dump、未迁移、未改代码/库/数据。
- 状态：迁移前环境就绪，等待用户保存 SOURCE_SECRET_KEY 后再进入 Phase 3.10 正式迁移。

---

## Phase 3.10-B.1 pg_dump 故障诊断（只读，本阶段 STOP，未执行 dump）
- 目标：查明为何 pg_dump 18.6 能连 Render PG 18.4 却不生成有效 custom dump。严格只读诊断。
- **第一步（客户端版本）PASS**：pg_dump/pg_restore/psql 均为 PostgreSQL 18.6（符合预期，18.6>18.4 主次版本兼容，非阻塞）。
- **第二步（DATABASE_URL 继承检查）**：`$env:DATABASE_URL` 在当前（沙箱）PowerShell 会话 = **UNSET**。本阶段 §九 红线明确「若 PowerShell 无法继承 DATABASE_URL：停止，不得自行寻找绕过方案」；且上一条用户指令禁止以临时文件/注册表/Bash·Python 桥接搬运 DATABASE_URL。
- **结论 = 本阶段无法推进**：连接测试（Step4）及后续 schema-only / 单表 dump 诊断（Step5-8）全部依赖连接串，而被禁止桥接、且环境未继承 → 必须立即 STOP，不尝试任何绕过。错误分类 = **H（POWERSHELL_EXECUTION_ERROR / 环境约束）**：问题在「诊断会话无法获取 DATABASE_URL」，而非 pg_dump 运行时错误。
- **未完成 dump 状态回顾**：`pg_backups/genius_trader2_0_20260818.dump` 仍 0 字节（上一轮 Phase 3.10-B 失败产物），已在上轮清理中标记为「未完成备份」，未删除、未 restore。
- **可继续条件**：在拥有 DATABASE_URL 的会话（用户本机 CMD/PowerShell，或用户显式一次性传入）内重跑 Step4-8；或用户授权在不落盘/不输出 URL 前提下由我持只读连接串诊断。当前红线下本阶段无法推进完整 pg_dump。
- 交付：诊断报告 `pg_diagnostics/diag_report.txt`（仅版本+URL_UNSET，无 secret）。总进度维持 **94%**｜目标：小程序可用。
- 红线遵守：未执行 pg_dump/pg_restore、未创建 Neon、未改 DATABASE_URL/SOURCE_SECRET_KEY/Render、未改代码/库/数据、未部署、未迁移、未输出任何 secret/连接串。

## Phase 3.10-B.2 本机 CMD 诊断（同被环境阻断，新增沙箱约束）
- 用户要求用「本机已设 DATABASE_URL 的 CMD」跑只读连接测试 + pg_dump 诊断（schema-only / 单表 fund·fund_nav·fund_holdings / pg_restore --list）。
- **沙箱新约束（实测）**：从 Bash 工具调用 `cmd.exe /c "..."` 被安全策略直接拦截 —— "Invoking cmd.exe from Bash bypasses all command validation"。故无法用 Bash 拉起原生 CMD。
- **环境继承断点（与 B.1 同）**：agent 工具子进程（PowerShell / Git Bash）均不继承用户在**交互式 CMD** 里设的 DATABASE_URL（B.1 实测 PowerShell `$env:DATABASE_URL`=UNSET；Git Bash 同源）。用户「CMD 已设置」指其本人会话，与 agent 沙箱不互通。
- **结论**：B.2 与 B.1 卡在同一点——执行环境无 DATABASE_URL，且用户明令禁止注册表/文件桥接。按 §九 立即停止，未切换 shell、未桥接、未跑实际诊断。错误分类 = H（CMD_EXECUTION_ERROR / 环境约束）。
- **可继续条件（不变）**：① 用户在其本人 CMD 跑 Step3-8 并把结果回贴；② 用户显式一次性传入连接串（运行期、不落盘/不输出）授权只读诊断。当前红线下本阶段无法由 agent 推进。
- 红线遵守：未调 cmd/PowerShell/GitBash 跑诊断、未桥接 DATABASE_URL、未执行 pg_dump/pg_restore、未改库/环境变量/配置、未输出 secret。
