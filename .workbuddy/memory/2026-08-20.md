# 2026-08-20 项目日志

## Phase 3.17-E：港美备用历史数据源单只接入验证（PASS，未写库未批量）

**目标**：把 Phase 3.17-D 选定的免 Key 源接入 `fetchStockHistory()` —— 港股=腾讯 `hk<5位>`（tencent-hk-kline）、美股=Nasdaq API（nasdaq-kline），并验证与现有 `stock_price` 统一 OHLCV 格式兼容。

**代码修改（仅 `server/services/marketService.js`）**：
1. `fetchText` 增加可选 `extraHeaders`（默认行为不变），Nasdaq 请求传 `Origin: https://www.nasdaq.com`。
2. 抽取公共 `fetchStockHistoryTencentSymbol(symbol, days)`（A股 sh/sz 与港股 hk 共用解析，不复制新工具）；`fetchStockHistoryTencent` 改薄包装（A股链路行为不变）；新增 `fetchStockHistoryTencentHk(code, days)` → `hk<code>`。
3. 新增 `fetchStockHistoryNasdaq(ticker, days)`：限速 `NASDAQ_MIN_INTERVAL_MS=1500`（防风控）、`MM/DD/YYYY→YYYY-MM-DD`、`$`/千分位逗号清洗（`toFiniteNumber`）、null 剔除；`errorLabel: 'Nasdaq 数据请求失败'`。
4. `fetchStockHistory` sources 构建改按市场路由（Phase 3.17-B 防误同步逻辑保留）：
   - A股（6 位数字）→ tencent → sina → eastmoney（不变，绝不进 Yahoo/Nasdaq）
   - 港股（5 位纯数字）→ tencent-hk → eastmoney(116.) → yahoo
   - 美股（字母 ticker ≤10 字符）→ nasdaq → eastmoney(105./106./107.) → yahoo
   - 日韩（.T/.KS 含 000660/285A/JP3236330001）→ 只走 Yahoo（不变）
   - 某源成功立即返回，不继续请求后续源。

**验证结果**：
- mock 验收 `phase3_17_e_verify.js`：**34/34 ALL PASS**（A 港股成功/fallback、B 美股成功/fallback/volume 清洗、C A股回归无 Yahoo/Nasdaq、D 日韩只走 Yahoo 零误同步、E Nasdaq 限速 ≥1400ms、F 错误隔离 Nasdaq 403 不混入东方财富文案、G 数据完整性升序/去重/OHLCV/日期格式）。初跑 5 个 FAIL 均为验收脚本断言缺陷（hits 去 query、volume 取错行、顶层返回值只有 all_sources_failed 需查日志），修正工具后全过，生产代码零回退。
- 真实网络单只验证（沙箱）：`01787 → tencent-hk-kline 60 条（2026-05-27~08-20，close≈25.16/25.96 港币）`；`AMD → nasdaq-kline 60 条（2026-05-26~08-19，close≈503.89/466.42 美元）`；`000408 → tencent-kline 60 条（A股回归不变）`；均升序、0 重复、0 非法 OHLCV。
- 沙箱真实网络验证成立（腾讯港 hk01787 与 Nasdaq 单发在 D 阶段已确认可达；Yahoo 仍 403 不阻塞，因为港美主源已替换）。

**红线**：未写库、未调 syncStockHistory、未批量同步、未改表结构/DATABASE_URL/SOURCE_SECRET_KEY、未删 Yahoo fallback、未扩展日韩同步规则、A股链路未动。

**下一步（Phase 3.17-F 门槛）**：01787 腾讯港股真实数据 PASS ✓、AMD Nasdaq 真实数据 PASS ✓、000408 A股回归 PASS ✓、港股 fallback PASS ✓、美股 fallback PASS ✓、数据格式 PASS ✓、写库=0、syncStockHistory=0 → 可进入 F：3–5 只港美小批量验证（届时仍需用户本机或确认后执行，沙箱不写库）。

## Phase 3.17-F：港美历史数据源 5 只小批量真实验证（PASS，未写库未批量）

**目标**：真实网络顺序验证 5 只港美股票 `fetchStockHistory({days:60})`，确认 3.17-E 接入的腾讯港股+Nasdaq 在小批量下稳定、来源正确、限速达标。

**真实网络测试清单**：
- 01787 [HK] → tencent-hk-kline | 60 条 | 2026-05-27~08-20 | PASS（fund 010827，真实持仓）
- AMD [US] → nasdaq-kline | 60 条 | 2026-05-26~08-19 | PASS
- TSM [US] → nasdaq-kline | 60 条 | 2026-05-26~08-19 | PASS
- ASML [US] → nasdaq-kline | 60 条 | 2026-05-26~08-19 | PASS
- 06869 [HK] → tencent-hk-kline | 60 条 | 2026-05-27~08-20 | PASS（第5只：真实持仓 funds 011452/025422/026211/501205；report_date 待 G 阶段从 fund_holdings 只读读取，沙箱无 Neon 直连）

**质量检查**：5 只均日期升序、0 重复、0 null close、close>0、high≥max(open,close,low)、low≤min(open,close,high)、volume≥0。
**Nasdaq 限速**：3 次请求间隔 1670ms / 1511ms，minGap=1511ms ≥1500ms（PASS）。
**来源正确性**：US 误走 tencent=0、HK 误走 nasdaq=0。
**fallback mock**：美股(Nasdaq→东财→Yahoo)、港股(腾讯港→东财→Yahoo) 两条链均 source=yahoo-kline，PASS，未破坏小批量代码。
**数据库安全**：脚本不 import dbAsync、不调 syncStockHistory；DB writes=0、stock_price writes=0、syncStockHistory=0。

**判定**：PHASE 3.17-F PASS。允许进入 Phase 3.17-G（生产 stock_price 小批量写入验证），但需用户先确认；本阶段绝不自动执行 G，禁止 34 只正式批量同步。

**红线遵守**：marketService.js 零改动（复用 3.17-E 代码）、未写库、未调 syncStockHistory、未批量同步、未改表结构/DATABASE_URL/SOURCE_SECRET_KEY、未删 Yahoo fallback、A股/日韩逻辑未动、未新增 Key。

## ⚠️ 关键环境修正：沙箱实际具备生产 DB 直连能力

此前多阶段（3.17-A~F）一直假设「沙箱无 Neon 直连、写库交用户本机」。本次 Phase 3.17-G 实测推翻该假设：
- 沙箱 Bash 环境**已注入 `DATABASE_URL`（生产 Neon PostgreSQL）**，`dbAsync.isCloud()=true`，可真实读写生产库；且出站可达腾讯港 / Nasdaq。
- 因此 G 阶段实际在沙箱直接完成了真实写入（非用户本机执行）。后续阶段如需写库，沙箱可直接做，不必再走「产出脚本交用户本机」。
- **唯一限制**：Bash 默认 120s 超时。连续多只 Nasdaq 请求（每只 stock 内部 fetch+sync 双请求 + 1500ms 限速 + Nasdaq 出站偏慢）易累计逼近 120s 被进程杀死（日志截断、exit≠0）。对策：用 `G_ONLY=<code>` 单只分批跑，单只仅 2 次 Nasdaq 调用，远低于超时。

## Phase 3.17-G：stock_price 生产小批量写入验证（PASS，真实写入 5 只）

**脚本**：`C:\pg_backups\phase3_17_g_write.js`（复用现有 `syncStockHistory(单code)`，内部 `upsertStockPrices` 走 `ON CONFLICT(stock_code,date) DO UPDATE`，幂等；支持 `G_ONLY=<code>` 单只、`G_REPEAT=1` 幂等复跑）。

**真实 stock_price 表结构（production information_schema 实测 + dbAsync.js DDL）**：
`id SERIAL PK, stock_code TEXT NOT NULL, date TEXT NOT NULL, price REAL NOT NULL, change_percent REAL, created_at TIMESTAMPTZ NOT NULL, updated_at TIMESTAMPTZ NOT NULL, UNIQUE(stock_code,date)`。
**无 `trade_date/open/high/low/volume/source` 列**——采集 OHLCV 入库时折叠为 `price(=收盘)+change_percent`；`source` 不落库，仅记录在报告中（符合用户「无 source 列则不改表、只记录」。）。

**写入结果（每只 fetchStockHistory({days:60}) → syncStockHistory → 验收）**：

| 股票 | 市场 | 数据源 | 写入前 | 写入后 | 新增 | 日期范围 | 重复 | null | 数据比对 | 结论 |
| --- | --- | --- | ---: | ---: | ---: | --- | :---: | :---: | --- | :---: |
| 01787 | HK | tencent-hk-kline | 0 | 60 | 60 | 2026-05-27~08-20 | 0 | 0/0 | 60/60 精确一致 | PASS |
| 06869 | HK | tencent-hk-kline | 0 | 60 | 60 | 2026-05-27~08-20 | 0 | 0/0 | 60/60 精确一致 | PASS* |
| AMD | US | nasdaq-kline | 0 | 60 | 60 | 2026-05-26~08-19 | 0 | 0/0 | 60/60 精确一致 | PASS |
| TSM | US | nasdaq-kline | 0 | 60 | 60 | 2026-05-26~08-19 | 0 | 0/0 | 60/60 精确一致 | PASS |
| ASML | US | nasdaq-kline | 0 | 60 | 60 | 2026-05-26~08-19 | 0 | 0/0 | 60/60 精确一致 | PASS |

\* 06869 数据正确，但**当前 `fund_holdings` 已无 06869**（见下「持仓核对」）。

**重复执行测试（幂等）**：`G_REPEAT=1` 仅复跑 01787 → 复跑前 cnt=60、复跑后 cnt=60、是否重复=否 → PASS。
**污染检查（直接等值计数，只读）**：`000408=0 / 000660=0 / 285A=0 / JP3236330001=0`；全表 `stock_price 总行数=300、不同 stock_code=5`（恰为 5 目标×60），无任何 `sz/sh` 前缀异常 → PASS。
**A2 安全检查**：仅 5 只变动，范围受控；未改表结构、未改 fund_holdings/fund_nav/账户/Token、未调 syncFundHoldingsHistory/syncAllHoldingsHistory（不扫描全表）。

**持仓核对（只读，重要发现）**：
- `01787` 确为真实当前持仓：fund `010827`，report_date `2026-06-30`。✓
- `06869` **当前 `fund_holdings` 不存在**（精确匹配 0 行；全表 5 位港股仅 10 种：01347/01787/01818/00981/09988/01138/02476/09899/00883/00522）。Phase 3.17-A 审计 JSON 标注的「06869 ∈ {011452,025422,026211,501205}」已**过期/失准**——当时快照与当前生产持仓已漂移。
- 含义：06869 的 stock_price 数据本身正确（无害的校准参考行），但它不满足用户「第5只须来自当前真实持仓」的硬要求。按用户兜底规则（无第二只港股则测 4 只即可），本应只测 4 只；因沿用过期审计选了 06869，**已如实标注，待用户决定**：(a) 保留 06869（无害）或 (b) 改写某只真实当前港股（如 01818/09988）以严格合规——但 (b) 会写入第 6 个代码，需用户明确授权（违反「禁止其他股票」红线前须确认）。

**附加观察（非 G 范围，仅供用户知悉）**：本 Neon PG 的 `stock_price` 仅 300 行（5×60），Phase 3.17-A 那一拨 119 只 A股×~400 行**不在此库**（大概率当时写到了旧 Render PG 或本地 SQLite）。属 3.17-A 执行落点问题，不影响 G 判定。

**判定**：PHASE 3.17-G PASS（5/5 真实请求成功、每只≥60、来源正确、OHLCV→price/change_percent 一致、日期升序、无重复、无非法、Nasdaq 未严重限流、DB 写入受控=300 行仅 5 代码、syncStockHistory 仅按单 code 调用）。

**是否允许进入 Phase 3.17-H**：YES（但绝不自动执行；H 为 34 只正式历史价格同步，须用户再行确认；且 G 发现的 06869 持仓失真问题建议先定夺）。

## Phase 3.17-H0：当前生产目标股票重新审计（只读，未写库未同步）

**脚本**：`C:\pg_backups\phase3_17_h0_audit.js`（复用 dbAsync 仅 SELECT fund_holdings；结果落 `phase3_17_h0_result.txt/.json`）。红线全守：未写 stock_price、未调 syncStockHistory、未改任何结构/表/账户/Token。

**当前 fund_holdings 去重分类（生产 Neon 实测）**：
- A股：105 只（3.17-A 名义已同步，但本 Neon PG 实际仅 300 行=G 的 5 只，A股同步不在此库——见 G 附加观察；H 范围外）
- 港股 HK：10 只 → 00522, 00883, 00981, 01138, 01347, 01787, 01818, 02476, 09899, 09988
- 美股 US：19 只 → AMD, ASML, AVGO, COHR, GLW, GOOG, GOOGL, INTC, LITE, MPWR, MU, NFLX, NVDA, SNDK, STX, TER, TSEM, TSM, WDC
- 日韩 JP：2 只 → JP3236330001, JP3684400009（外加 285A 存为裸码归 OTHER，亦 JP）
- 日韩 KR：2 只 → 000660, 005930
- 其他：1 只 → 285A（Sony，JP，归 OTHER；H 排除）

**与计划基线（3.17-A 审计 34 只港美 = 11 HK + 23 US）差异**：
- 当前仍存在（计划∩当前）= 24 只（HK 8 + US 16）
- 已不在持仓（计划-当前）= 10 只：01888, 02590, 06869（HK）；AEIS, AXTI, CIEN, KLAC, LRCX, MRVL, VIAV（US）——基金调仓后 fund_holdings 已无记录
- 新增（当前-计划）= 5 只：00981, 01347（HK）；MPWR, NFLX, STX（US）——审计后新买入/新披露
- 变化原因：季报调仓（减） + 审计后新持仓（增）

**06869 处置（按红线）**：已在 G 写入 stock_price 60 条，当前 fund_holdings 已无 → 不删除、不修改、不纳入 H、不新增替代。

**最终 H 正式同步目标 = 港股 10 + 美股 19 = 29 只**（非原计划的 34，因 10 只已不在持仓、5 只新增）。29 只全部可经现有 3.17-E 路由（HK→腾讯港、US→Nasdaq），无日韩（Yahoo 403 排除）。

**判定**：H0 完成（只读）。等待用户确认 H0 名单后，方可进入 Phase 3.17-H1 正式同步（绝不自动执行）。

## Phase 3.17-H1-A：10 只港股 stock_price 正式同步（部分完成，验收 FAIL）

**脚本**：`C:\pg_backups\phase3_17_h1a.js`（从 `phase3_17_h0_result.json` 程序化读取 `current_by_market.HK` 去重得 10 只；每只先只读复核仍在 fund_holdings，再顺序调现有 `syncStockHistory(code)`（默认 365 天、幂等 upsert、全或无事务）；写后只读验收）。`phase3_17_h1a_verify.js` 为最终只读验收（仅 SELECT，落 `phase3_17_h1a_verify.json`）。

**天数决策**：未覆盖 syncStockHistory 默认 365 天（"existing" 函数默认），用于正式校准深度 ≈1 年交易日（非 G 的 60）；01787 从 G 的 60 条扩展至 365（幂等重叠段 UPDATE，+305）。

**运行实况**：沙箱 Bash 前台 120s 超时杀进程 → 改用 `run_in_background` 跑完。但用户在进程跑到第 9 只（09899）后**手动结束进程**，导致第 10 只 `09988` 从未开始（日志与 DB 均证实 09988=0 行）。港股经腾讯港出站在沙箱偏慢（~2min/只），10 只总耗时超前台 120s，故必须后台或 `H1A_ONLY` 单只分批。

**最终验收（只读核对生产 Neon 实际状态）**：

| 股票 | source | fetched | 前cnt | 后cnt | 新增 | 日期范围 | null/dups/≤0 | cp范围 | 结论 |
| --- | --- | ---: | ---: | ---: | ---: | --- | :---: | --- | :---: |
| 00522 | tencent-hk-kline | 365 | 365 | 365 | 0 | 2025-02-26~2026-08-20 | 0/0/0 | -18.7%~+12.0% | PASS（幂等）|
| 00883 | tencent-hk-kline | 365 | 0 | 365 | 365 | 同上 | 0/0/0 | -15.3%~+5.9% | PASS |
| 00981 | tencent-hk-kline | 365 | 0 | 365 | 365 | 同上 | 0/0/0 | -16.3%~+12.7% | PASS |
| 01138 | tencent-hk-kline | 365 | 0 | 365 | 365 | 同上 | 0/0/0 | -12.2%~+19.1% | PASS |
| 01347 | tencent-hk-kline | 365 | 0 | 365 | 365 | 同上 | 0/0/0 | -16.4%~+17.9% | PASS |
| 01787 | tencent-hk-kline | 365 | 60 | 365 | 305 | 同上 | 0/0/0 | -14.3%~+11.1% | PASS |
| 01818 | tencent-hk-kline | 365 | 0 | 365 | 365 | 同上 | 0/0/0 | -10.4%~+10.2% | PASS |
| 02476 | tencent-hk-kline | 84 | 0 | 84 | 84 | 2026-04-21~2026-08-20 | 0/0/0 | -16.3%~+16.6% | PASS（次新上市，天数少属正常）|
| 09899 | tencent-hk-kline | 365 | 0 | 365 | 365 | 2025-02-26~2026-08-20 | 0/0/0 | -12.9%~+10.4% | PASS |
| 09988 | — | 0 | 0 | 0 | 0 | — | — | — | **FAIL（进程结束前未写入）** |

**合计新增 stock_price 行数（10 只相对 H1-A 前）** = 2944 行（00522 365 + 00883/00981/01138/01347/01818/09899 各 365×6=2190 + 01787 +305 + 02476 +84；09988 贡献 0）。

**额外只读检查（全部 PASS）**：
- 00522 重跑幂等：before=365→after=365，new=0 → 保持幂等 ✓
- 06869：cnt=60（range 2026-05-27~08-20）未改动 ✓
- AMD/TSM/ASML：各 cnt=60 未改动 ✓
- 污染码 000408/000660/285A/JP3236330001：各 =0，无异常记录 ✓
- stock_price distinct codes = 恰 {10 HK + 06869 + AMD/TSM/ASML} 14 个，无 sz/sh 前缀、无跨市场误路由 ✓
- fund_holdings=202 / fund_nav=32834 / users=5（与文档基线完全一致）→ 未改动 ✓
- 是否写入生产 Neon：是（同一 DATABASE_URL 生产库 SELECT 证实；00522 跨两次运行持久化至 365 进一步证实）✓

**判定**：**PHASE 3.17-H1-A FAIL**（唯一失败项 = `09988` 未写入，因用户手动结束后台进程前该股票未轮到；非数据错误、非跨市场污染、非结构改动）。

**按用户 FAIL 协议**：只报告失败项，**不自行修复、不重跑 09988**。H1-A 验收后停止，**绝不自动进入 H1-B**。

**待用户决定**：09988 仅缺失一只，是否授权单只补写（建议 `H1A_ONLY=09988` 单只跑，远低于超时），或整体重跑 H1-A。美股 19 只（H1-B/C）仍待 Phase 3.17-H1-B 确认后启动。

## Phase 3.17-H1-A 补写 09988（数据已存在，幂等复验确认正确）

**用户授权**：仅补齐唯一失败项 09988，红线全守（只动 09988、用现有 syncStockHistory、不碰其他股票/不改 marketService.js/不改表-holdings-nav-账户-token、不进 H1-B）。

**脚本**：`C:\pg_backups\phase3_17_h1a_09988.js`（先只读输出 09988 当前 count → 仅 09988 调 `syncStockHistory`（默认 365 天）→ 只读验收 → 同步落 `phase3_17_h1a_09988_verify.json`）。

**实测结果（重要修正前文 FAIL 判定）**：
- **执行前只读 count = 365** —— 即 `09988` 在被本脚本写入前**已存在 365 条正确行**。说明此前 H1-A 后台进程（sBNfrO）被用户在跑到第 9 只后「结束」时，实际仍在沙箱继续跑完并写入了 09988；而我的 H1-A 最终只读验收（phase3_17_h1a_verify.js）恰好在那之前执行，才看到 09988=0 的瞬时状态。即 09988 其实已被那段 lingering 进程完成，非真正缺失。
- 本脚本对 09988 幂等 `syncStockHistory` 复验：source=`tencent-hk-kline`、fetched=365、before=365→after=365（**无重复、新增 0**）、日期范围 `2025-02-26~2026-08-20`、**null price=0 / duplicate date=0 / price≤0=0**、change_percent 范围 `-17.98%~+18.50%（异常 0）`、maxgap=6 天（连续）、**跨市场误路由=NO**、**modified_other_codes=NONE**、distinct codes 未变、total 行数 delta=0。
- 自动化 verdict 字段虽因 `newRows≠fetched`（期望新增 365 实为 0）标了 FAIL，但这是**过严判据的误报**——数据本身完整且正确、零污染、零重复。

**修正判定**：09988 数据完整正确（365 条 tencent-hk-kline、质量全绿、隔离干净）→ **H1-A 实际数据完整性已满足，原 FAIL 因瞬时快照误判**。10 只港股全部到位、合计 10×365（02476 为 84 次新）行正确数据，**H1-A = PASS（数据闭环）**。美股 19 只仍未动，待 H1-B 确认。

## Phase 3.17-H1-B：5 只美股 stock_price 正式同步（PASS，数据闭环）

**用户授权**：仅 AMD/ASML/AVGO/COHR/GLW 五只；严格串行（AMD→ASML→AVGO→COHR→GLW）；复用现有 Nasdaq 主路由（Nasdaq→东财→Yahoo）+ syncStockHistory（默认 365 天、幂等 upsert）；Nasdaq 串行 ≥1500ms（代码内 NASDAQ_MIN_INTERVAL_MS 强制）；不碰其他股票/日韩/剩余14美股、不改 marketService.js/表/holdings/nav/账户/token、不调全量同步、不进 H1-C。

**脚本**：`C:\pg_backups\phase3_17_h1b.js`（串行同步+逐只只读验收+锚点快照防越权+Nasdaq 间隔测量+AMD 幂等抽查；落 `phase3_17_h1b_verify.json`）。后台运行（沙箱 Nasdaq 出站偏慢，5 只总耗时 ~15min，远超前台 120s）。

**逐只结果（全部 PASS）**：

| 股票 | source | fetched | 前cnt | 后cnt | 新增 | 日期范围 | null/dup/≤0 | cp范围 | 跨市场 |
| --- | --- | ---: | ---: | ---: | ---: | --- | :---: | --- | :---: |
| AMD | nasdaq-kline | 365 | 60 | 365 | 305 | 2025-03-07~2026-08-19 | 0/0/0 | -17.3%~+23.8% | NO |
| ASML | nasdaq-kline | 365 | 60 | 365 | 305 | 同上 | 0/0/0 | -8.3%~+15.4% | NO |
| AVGO | nasdaq-kline | 365 | 0 | 365 | 365 | 同上 | 0/0/0 | -12.6%~+18.7% | NO |
| COHR | nasdaq-kline | 365 | 0 | 365 | 365 | 同上 | 0/0/0 | -20.2%~+22.7% | NO |
| GLW | nasdaq-kline | 365 | 0 | 365 | 365 | 同上 | 0/0/0 | -13.6%~+15.7% | NO |

**独立只读验收（`phase3_17_h1b_accept.js`，纯 SELECT，落 `phase3_17_h1b_accept.json`）**：
- 5 只全部到位、质量全绿（null=0/dup=0/price≤0=0/cp 异常=0）。
- 剩余 13 美股（GOOG/GOOGL/INTC/LITE/MPWR/MU/NFLX/NVDA/SNDK/STX/TER/TSEM/WDC）= 0，无越权新增 ✓
- 既有持仓（10 HK + 06869 + TSM=60）保持不变 ✓
- stock_price distinct codes = 17、total = 5314（前态 3609 + Δ1705：AMD/ASML 各 +305、AVGO/COHR/GLW 各 +365）✓
- Nasdaq 串行间隔实测 142902 / 159210 / 149979 / 143989 ms（代码内 1500ms 强制 + 处理耗时，远超下限）✓
- 幂等抽查：AMD 重跑 before=after（365→365）✓

**自动化 verdict 误报说明（重要）**：主脚本 `phase3_17_h1b.js` 内 `final.verdict` 标了 FAIL，但属**脚本逻辑缺陷误报**，非数据失败：
1. 锚点 delta 检查引用了 SCOPE 五码，但 `watch` 映射漏列这五码 → `undefined-undefined=NaN` → `delta_ok=false`。
2. 独立验收脚本首版把 TSM（G 阶段已写 60）错误列入「应为 0」清单 → `other_us_zero=false`；修正后（排除 TSM）即 PASS。
两处均为验收脚本断言缺陷，数据本身完整正确、零污染、零重复、零误路由、零越权。

**修正判定**：**PHASE 3.17-H1-B = PASS（数据闭环）**。已写入生产 Neon，美股累计已落 6 只（AMD/ASML/AVGO/COHR/GLW/TSM）。剩余 13 美股待 H1-C 授权；绝不自动进入 H1-C。

## Phase 3.17-H1-US-REMAINING：剩余 13 只美股正式同步（PASS，数据闭环）

**用户授权**：一次性处理剩余 13 美股（GOOG/GOOGL/INTC/LITE/MPWR/MU/NFLX/NVDA/SNDK/STX/TER/TSEM/WDC），不拆 H1-C/D/E；严格串行 Nasdaq 主路由（→东财→Yahoo）+ syncStockHistory（默认 365 天、幂等 upsert）；相邻 Nasdaq ≥1500ms（代码内强制）；不碰 AMD/ASML/AVGO/COHR/GLW/TSM/港股/A股/日韩、不改 marketService.js/表/holdings/nav/users/token、不删已有数据、不调全量同步。

**脚本**：`C:\pg_backups\phase3_17_us_remaining.js`（串行同步 13 只 + 逐只只读验收 + 锚点快照防越权 + Nasdaq 间隔测量 + GOOG 幂等抽查；落 `phase3_17_us_remaining_verify.json`）。后台运行（13 只串行 Nasdaq 总耗时 ~33min，远超前台 120s）。

**逐只结果（全部 PASS）**：13 只均 `source=nasdaq-kline`、`fetched=365`、`before=0→after=365`、`new=365`、日期 `2025-03-07~2026-08-19`、`null=0/dup=0/price≤0=0/cp异常=0`（各股 cp 范围均在 ±28% 内）、`cross_market=NO`、`error=none`。

**主脚本终判**：`H1-US-REMAINING = PASS`；Nasdaq 最小间隔=127663ms（代码内 1500ms 强制 + 串行，远超下限）；changed_others=NONE（无越权改其他股票）；totalDelta=4745 = sumNew=4745；GOOG 幂等重跑 365→365。

**独立只读验收（`phase3_17_us_remaining_accept.js`，纯 SELECT，落 `phase3_17_us_remaining_accept.json`）**：ACCEPT VERDICT = PASS；distinct codes=30（13 scope + 17 held：10 HK/06869/AMD/ASML/AVGO/COHR/GLW/TSM）、total rows=10059（前态 5314 + Δ4745）；HELD 各码计数与既有基线完全一致（06869=60、TSM=60、AMD/ASML/AVGO/COHR/GLW=365、10 HK 一致）；污染码 000408/000660/285A/JP3236330001=0；fund_holdings=202/fund_nav=32834/users=5 未变。A~L 全绿。

**判定**：**PHASE 3.17-H1-US-REMAINING = PASS（数据闭环）**。美股 19 只至此全部到位（G:TSM + H1-B:AMD/ASML/AVGO/COHR/GLW + 本阶段 13 只）。按用户红线：**不自动进入最终验收**，等待下一条指令进入 Phase 3.17-H1-F 最终总验收。

**环境备注（延续）**：沙箱 Bash 已注入 `DATABASE_URL`（生产 Neon PG），可真实读写；仅前台 120s 超时限制长耗时串行任务（用 run_in_background 规避）。`node:sqlite` 的 ExperimentalWarning 仅为 import 期提示，实际走 PG（isCloud()=true）。

## Phase 3.17-H1-F：最终总验收（只读，PASS，数据闭环）
**脚本**：`C:\\pg_backups\\phase3_17_h1f_accept.js`（纯 SELECT，复用 dbAsync；落 `phase3_17_h1f_accept.json` + 报告 `小程序/.workbuddy/Phase3.17_H1F_final_acceptance.md`）。前置 H1-A / H1-B / US-REMAINING 均 PASS，本阶段仅做只读总验收。

**验收项（全绿）**：
- A. 30 目标代码计数全部达标：28×365 + 02476(84) + 06869(60) + TSM(60)。港股 11（00522/00883/00981/01138/01347/01787/01818/02476/06869/09899/09988）、美股 19（AMD/ASML/AVGO/COHR/GLW/GOOG/GOOGL/INTC/LITE/MPWR/MU/NFLX/NVDA/SNDK/STX/TER/TSEM/TSM/WDC）。
- B. 每码质量：null_price=0 / price_le0=0 / dup(date)=0 / null_cp=0 / cp_abn(±30%)=0。
- C. 跨市场完整性：distinct=30 == 白名单 30，缺失=无、意外/泄漏代码=无（含 A股 6 位码专项检查）。
- D. 污染码排除：000408 / 000660 / 285A / JP3236330001 全部 = 0。
- E. 基表不变：fund_holdings=202 / fund_nav=32834 / users=5（与基线完全一致）。
- F. 总行数一致性：total=10059 == 各码期望和 10059 == 基线 10059。

**判定**：**PHASE 3.17-H1-F = PASS（最终总验收通过，数据闭环完整）**。
**红线遵守**：未写库、未调 syncStockHistory、未改代码/表结构/DATABASE_URL/SOURCE_SECRET_KEY、未碰 A股/日韩逻辑、无越权、无污染。
**结论**：Phase 3.17 港美历史价格同步主线（E 接入 → F 小批量 → G 写入 → H0 审计 → H1-A 港股 → H1-B 美股5 → US-REMAINING 美股13 → H1-F 总验收）全部 PASS，可正式收口。下一步建议：如需将股票历史价格接入小程序展示层或建立自动同步调度，再开新 Phase；本阶段绝不自动执行。

## 「统一整改 TODO」P0 估值/净值链路（双端修复完成，静态+API 验证 PASS）

**根因（P0-1~3）**：持仓列表的净值来自 `fund_nav`（按 code 逐只刷新，非同步时写入）；估值数据源标识的判定缺陷在**前端渲染层**：网页端 `.nav-estimate-badge` 是灰（#86868b）、`markEstimateBadge` 把 provider 名（小倍/养基宝）渲染成灰；mp1 `providerDisplayName` 只认长名（xiaobeiyangji/yangjibao），列表却传短名（xbyj/yjb）→ provider 标签从未解析成功；且「灰估值」在 navDate≠today 时无条件显示，未区分"有估值数据/无数据"。

**修复（四态判定，双端对齐）**：
- mp1 `utils/tradingDay.js`：`providerDisplayName` 兼容短/长名；`computeDataBadge` 重写为四态：①蓝「已更新MMDD」（navDate===expected 且官方涨跌幅 finite，QDII 用前一交易日）②非交易日有 navDate → 蓝 ③有估值数据（hasEstimateData）→ 蓝「provider名/估算」④唯一灰色场景 = 无净值无估值 → 灰「估值」。新增 `hasEstimateData` 参数。
- mp1 `pages/portfolio/portfolio.js:564-569`：调用点算 `hasEstimateData = Boolean(navDate) || finite(today) || finite(todayEstimate)` 传入；badge 挂 `item.dataBadge`；wxml:113 按 tone 映射 `data-badge-blue`/`data-badge-gray`；wxss `.data-badge-blue` #0071e3。
- 网页端 `live-estimates.js`：新增 `markDataSourceBadge`（蓝 `.nav-provider-badge` #0071e3）；统一决策块 526-532（有 finite change → 蓝数据源标识；无 → 灰「估值」）；无数据早期分支 500/510 灰「估值」；provider 追更 554 蓝。`layout.css:45` 新增 `.nav-provider-badge` 蓝规则。
- API 实测：`/api/fund/000001/estimate?mode=provider&source=xbyj/yjb` 与普通调用一致，无第三方数据时无 source 字段（前端回退「估算」，蓝）——判定链闭环。

**改动文件**：`小程序/mp1/utils/tradingDay.js`、`mp1/pages/portfolio/portfolio.js`、`mp1/pages/portfolio/portfolio.wxml`（无改动，样式类已存在）、`Codex3 基金/genius-trader2.0/live-estimates.js`、`layout.css`。均通过 node --check。
**待用户真机验收**：小程序真机/开发者工具下拉刷新、网页端 Ctrl+Shift+R，确认：有第三方数据 → 蓝「小倍/养基宝」；本地引擎有估值 → 蓝「估算」；完全无数据 → 灰「估值」。

## 「统一整改 TODO」P1 + P2（全部完成，语法/单测 PASS）

**P1 小程序持仓列表**（`portfolio.js/wxml`、新 `utils/fundSectors.js`）：
- 删除新增基金「分类」picker（wxml 表单行 + `onCategoryPickerChange` + data `pickerCategories/categoryIndex` 清理）。
- 系统自动识别板块：新共享模块 `utils/fundSectors.js`（`FUND_SECTORS_BY_CODE` 板块表与 analysis.js 页内常量对齐 + `sectorNameOf` + `assetClassOf` 大类识别：代码板块映射 → 名称关键词 → 历史 category → '其他'）；`submitAddFund` 用 `assetClassOf({code,name})`。单测 12/12 PASS（019633→权益类、004253→黄金类、013309→海外类、014847→债券类、债券/黄金/纳指名称关键词、null→其他）。
- 历史分类兼容：wxml 显示 `{{item.category || '其他'}}`。
- 默认排序（sortState='default' 多级）：基金名(zh-Hans-CN) → 今日收益 desc → 持有收益 desc → 持有金额 desc；表头点击循环排序逻辑不变。

**P1 小程序基金详情**（`components/fundDetail`、`pages/index`）：
- 交易金额 `minimumFractionDigits:2` → `maximumFractionDigits:2`（不强制补 0），新增 `feeStr`（wxml 用 `{{item.feeStr || item.fee}}`）。
- 指数点位（index.js:172）同改 `maximumFractionDigits:2`。
- 删除「快捷」文案（wxml quick-ratio-row 的 label），quick-ratio-row 统一左右 28rpx 内边距；`.form-grid` margin/padding 24→28rpx 统一。
- `.chart-section` 删除 `position:sticky`（历史详情取消顶部固定，跟随滚动）。
- 校准逻辑（Phase 3.16）零改动（`toFixed(3)` MAE 保留）。`toFixed(4)` 均为净值/成本价行业标准保留。

**P1 网页端修改持仓弹窗**（`detail-api.js` + `holding-editor.css`）：
- 删辅助说明 `<p>`；Tab 改「修改|加仓|减仓|定投|清仓」顺序+文案，`.holding-action-switch` grid 3 列→5 列一行（媒体查询也保持 5 列，字号 11px）。
- 「快捷金额」文案删除，快捷比例移到买入/卖出时间 grid **上方**。
- `.holding-editor` 隐藏滚动条（`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`）= 删除右侧滑条。

**P2 网页端基金详情**（`detail-api.js` + `layout.css`）：
- 定投计划 banner 移到 record tabs（历史净值）上方；`updateAutoInvestBanner` 改为在 `.detail-record-section` 前插入/移除。
- 四 Tab 统一：历史净值|历史业绩|前十大持仓|交易记录 横排（`.detail-record-tabs` 已 flex）；`renderRecord` 扩展 4 分支；`backdrop._renderActiveRecord` 挂载供 startLoad/交易提交后重渲染；`applyMetaSideEffects` 将 `payload.holdings` 写回 `fund.holdings`；旧 `.detail-holdings-content/.detail-transaction-content` 查询保留为 else 兜底（安全）。
- 小程序仅调整 Tab 顺序（fundDetail.wxml：nav|performance|holdings|transactions），未重复改 UI。

**P2 网页端校准**（`detail-api.js` + `layout.css`）：
- 详情页 chart section 加「校准」按钮 + `.detail-calibration-result`（状态 已校准/样本不足 + 样本 N 日/方向准确率/MAE/RMSE/权重 持仓/行业/现金）。
- `triggerCalibration` 调 `GET /api/fund/:code/calibration?recalibrate=1`（与小程序同一接口/算法），成功后 `startLoad(forceRefresh)` 刷新估值。实测接口返回 `{calibration:{calibrated,holdings_weight,sector_weight,cash_adjustment,mae,rmse,direction_accuracy,sample_size,calibrated_at}}` 与实现匹配。

**P2 网页端备份**（`auth.js` + `data-menu.js`）：
- 右上角三点菜单移除「备份管理」项；`openBackupManager` 挂 `window` 供账号弹窗复用。
- 账号弹窗「备份云端」→「立即备份」，两个按钮下方新增「备份列表」按钮 → `window.openBackupManager()`（列表/恢复/删除/立即备份完整保留）。

**P2 网页端导出**（`data-menu.js`）：
- 「导出数据」点击先弹范围选择「全部数据|投资策略」（`.confirm-actions` 2 列 grid），`exportSnapshot(kind)` 生成对应 JSON（策略格式 `{kind:'strategy', strategies:[{name,strategy}]}`，与小程序一致）；文件名 `genius-trader-data|strategy-YYYY-MM-DD.json`。
- 导入兼容 `kind='strategy'`（`confirmImportStrategy` 仅合并 strategy 不覆盖持仓，确认弹窗 + 无匹配账户报错）。

**统一格式（双端金额/百分比）**：新 `utils/format.js`（`pct(rate01, sign)` / `money(val)`，最多 2 位不强制补 0，null/undefined/NaN → '—'）；替换 portfolio.js（holdingRateStr/todayProfitPctStr）、fundDetail.js（10 处百分比）、网页端 account-tabs.js / portfolio-summary.js / live-estimates.js money（`minimumFractionDigits:2`→`0`）与 percent（toFixed 去尾零）、detail-api.js percent。单测 19/19 PASS（含 `+5.5%`/`12%`/`10,000`/`—` 边界）。

**验证**：全部改动的 JS `node --check` PASS；网页端 8 个文件语法 OK。**待用户真机/浏览器验收**：小程序真机刷新（徽章色/默认排序/备份导出/详情 Tab 顺序/金额格式）、网页端 Ctrl+Shift+R（详情四 Tab/校准/备份弹窗/导出范围选择/金额格式）。后端零改动。
